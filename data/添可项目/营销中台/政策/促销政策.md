> HTML 页面: [[page/wiki/data/添可项目/营销中台/政策/促销政策.html|打开 HTML 页面]]

## sql
```sql
ALTER TABLE `dmp_marketing`.`ims_offline_promotion_policy` 
ADD COLUMN `per_unit_commission` decimal(10, 2) NULL COMMENT '单台导购提成(元/台)',
ADD COLUMN `sales_incentive_type` varchar(50) NULL COMMENT '销售激励类型',
ADD COLUMN `sales_target_min` int(11) NULL COMMENT '销售目标下限',
ADD COLUMN `sales_target_max` int(11) NULL COMMENT '销售目标上限',
ADD COLUMN `per_unit_incentive` decimal(10, 2) NULL COMMENT '单台销售激励(元/台)',
ADD COLUMN `is_included_bet_reward_scope` varchar(10) NULL COMMENT '是否计入对赌奖励规模Y/N',
ADD COLUMN `is_included_bet_reward_policy` varchar(10) NULL COMMENT '是否计入对赌奖励政策Y/N';
```

## GOMS配置快码
**SALES_INCENTIVE_TYPE**

```sql
NO_LADDER("无阶梯"),
    DIRECT_LADDER("阶梯直算"),
    SEPARATE_LADDER("阶梯分算");
```

