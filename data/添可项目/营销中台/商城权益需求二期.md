1.加字段

```
ALTER TABLE `dmp_wms`.`procurement_order_header` 
ADD COLUMN `interface_status_www` varchar(50) NULL DEFAULT NULL COMMENT '商城接口推送状态（Y成功/N失败）';
```

2.wms加配置

```
css:
  wwwExpireSn: https://t-esb-gateway.tineco.com/restcloud/electrical-mall/order/cityOrder/expireSnCodeBatch
  sendTCMsg: https://esb-gateway.tineco.com/restcloud/css/gateway/messageServer/tcMessageRecord/sendTCMsg
  sendTcRoomCode4Www: TCR10003
  sendTcRemindPeopleNo4Www: roomBot
```

3.tms加feign接口

```
@GetMapping(value = "imsLogisticsDeliveryOrderHeader/findDeliveryHeader4Sn")
    @ApiOperation("根据SN获取最新发货单或电商发运单")
    ResBody<ImsLogisticsDeliveryOrderHeaderDto> findDeliveryHeader4Sn(@RequestParam("sn") String sn);
```

4.tms加索引

```
ALTER TABLE `dmp_tms`.`ims_logistics_delivery_order_header` 
ADD INDEX `idx_deliver_status`(`deliver_status`) USING BTREE;
```