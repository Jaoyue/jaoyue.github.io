### 1.yml配置

```
finance:
  accountLockUrl: https://t-esb-gateway.tineco.com/restcloud/sys-marketing/gateway/financeServer/consumerGoodsCredit/occupyAndRelease
  appkey: 62f5f201bfd0f10d93e7fa19
customer:
  accountLockCustomerUrl: https://t-esb-gateway.tineco.com/restcloud/sys-marketing/gateway/customerServer/customerInfo/getAccountLockCustomer
  appkey: 62f5f201bfd0f10d93e7fa19
```

### 2.sql

```
ALTER TABLE `dmp_sales_order`.`ims_ec_sales_order_ext`
ADD COLUMN `account_lock_flag` INT(0) DEFAULT 0 NULL COMMENT '额度占用标识 0无需占用 1需占用 2占用成功 3占用异常' ,
ADD COLUMN `account_lock_msg` VARCHAR(500) NULL COMMENT '额度占用信息' ,
ADD COLUMN `account_unlock_flag` INT(0) DEFAULT 0 NULL COMMENT '额度释放标识 0待释放 2释放成功 3释放异常' ,
ADD COLUMN `account_unlock_msg` VARCHAR(500) NULL COMMENT '额度释放信息' ;
```

### 3.customer服务发版, 配置esb接口

[https://t-esb-gateway.tineco.com/restcloud/sys-marketing/gateway/customerServer/customerInfo/getAccountLockCustomer](https://t-esb-gateway.tineco.com/restcloud/sys-marketing/gateway/customerServer/customerInfo/getAccountLockCustomer)

### 4.ec服务发版

### 5.配置缓存开关

EC_CREDIT_ACCOUNT_LOCK_SWITCH