## 排期文档
[APP-开发计划-日清三期.xlsx](https://www.yuque.com/attachments/yuque/0/2025/xlsx/29172728/1740964501241-6702bf2e-a722-4673-ab3c-a032d277f069.xlsx)

## 上线sql
```sql
ALTER TABLE `platform-store`.`sp_user_msg_remind` 
ADD COLUMN `source_type` varchar(50) NULL AFTER `source_no`;
update sp_user_msg_remind set source_type = 'DAILY_TODO' where type in ('TODO_FINISH','TODO_REVIEW');
update sp_user_msg_remind set source_type = 'PATROL' where type in ('PATROL_FINISH','PATROL_REVIEW','PLAN_NOT_PASS');
ALTER TABLE `platform-store`.`sp_user_daily_task_stock` 
ADD COLUMN `stock_cost` varchar(50) NULL DEFAULT NULL COMMENT '库存估值';

ALTER TABLE `platform-store`.`sp_user_daily_task_stock_item` 
ADD COLUMN `purchase_price` double NULL DEFAULT NULL COMMENT 'toC单价',
ADD COLUMN `stock_cost` varchar(50) NULL DEFAULT NULL COMMENT '库存估值';
ALTER TABLE `platform-store`.`sp_user_msg_remind` 
MODIFY COLUMN `source_no` varchar(50) NULL DEFAULT NULL;
```

## 配置缓存
| <font style="color:rgb(81, 90, 110);">资源key</font> | sp_gift_audit_msg_user |
| --- | --- |
| <font style="color:rgb(81, 90, 110);">资源value</font> | 497 |
| <font style="color:rgb(81, 90, 110);">备注</font> | 赠品审核通知人 |


