## GOMS上线sql

```
create table ims_product_price_store_segment_header
(
    store_segment_header_id bigint                                                  not null comment '主键ID（雪花算法）'
        primary key,
    store_segment_code      varchar(64)                                             not null comment '申请单号（自动生成 PSA-...）',
    store_segment_name      varchar(200)                                            not null comment '单据名称',
    apply_user_id           int                                     default 0       not null comment '申请人id',
    apply_user_name         varchar(100) collate utf8mb4_general_ci default ''      not null comment '申请人名称',
    apply_department_id     int                                     default 0       not null comment '申请部门id',
    apply_department_code   varchar(100) collate utf8mb4_general_ci default ''      not null comment '申请部门编码',
    apply_department_name   varchar(100) collate utf8mb4_general_ci default ''      not null comment '申请部门名称',
    start_date              datetime                                                not null comment '生效起始时间',
    end_date                datetime                                                not null comment '生效终止时间',
    audit_status            varchar(20)                             default 'DRAFT' not null comment '审批状态: DRAFT/APPROVAL/ALLOW/REJECT',
    remark                  varchar(500)                                            null comment '备注',
    delete_flag             int                                     default 0       not null comment '逻辑删除标志 (0-正常, 1-已删除)',
    version_num             int                                     default 1       not null comment '乐观锁版本号',
    created_by              bigint                                                  not null comment '创建人',
    creation_date           datetime                                                not null comment '创建时间',
    last_updated_by         bigint                                                  not null comment '最后更新人',
    last_update_date        datetime                                                not null comment '最后更新时间',
    last_update_login       bigint                                                  null comment '最后更新登录ID'
)
    comment '门店商品价格段申请单头表'
    charset = utf8mb4;

create index idx_audit_status
    on ims_product_price_store_segment_header (audit_status);

create index idx_creation_date
    on ims_product_price_store_segment_header (creation_date);

create index idx_store_segment_code
    on ims_product_price_store_segment_header (store_segment_code);
    
create table ims_product_price_store_segment_product_line
(
    store_segment_product_line_id bigint                          not null comment '主键ID（雪花算法）'
        primary key,
    store_segment_header_id       bigint                          not null comment '外键 - 关联头表 store_segment_header_id',
    product_sap_id                varchar(64)                     not null comment '产品SAP ID (前端输入后自动带出)',
    product_name                  varchar(200)                    null comment '产品名称 (自动带出)',
    product_big_type              varchar(100)                    null comment '产品大类 (自动带出)',
    product_middle_type           varchar(100)                    null comment '产品中类 (自动带出)',
    product_small_type            varchar(100)                    null comment '产品小类 (自动带出)',
    price_min                     decimal(18, 2)                  not null comment '价格下限（元）',
    price_max                     decimal(18, 2)                  not null comment '价格上限（元）',
    abate_status                  varchar(20) default 'EFFECTIVE' null comment '失效状态: EFFECTIVE/INVALID',
    delete_flag                   int         default 0           not null comment '逻辑删除标志',
    version_num                   int         default 1           not null comment '乐观锁版本号',
    created_by                    bigint                          not null comment '创建人',
    creation_date                 datetime                        not null comment '创建时间',
    last_updated_by               bigint                          not null comment '最后更新人',
    last_update_date              datetime                        not null comment '最后更新时间',
    last_update_login             bigint                          null comment '最后更新登录ID'
)
    comment '门店商品价格段商品行表'
    charset = utf8mb4;

create index idx_product_sap_id
    on ims_product_price_store_segment_product_line (product_sap_id);

create index idx_store_segment_header_id
    on ims_product_price_store_segment_product_line (store_segment_header_id);
    
create table ims_product_price_store_segment_store_line
(
    store_segment_store_line_id   bigint                          not null comment '主键ID（雪花算法）'
        primary key,
    store_segment_header_id       bigint                          not null comment '外键 - 关联头表 store_segment_header_id（冗余字段）',
    store_segment_product_line_id bigint                          not null comment '外键 - 关联商品行 store_segment_product_line_id（核心关联）',
    store_code                    varchar(64)                     not null comment '门店编码 (前端输入后自动带出)',
    store_name                    varchar(200)                    null comment '门店名称 (自动带出)',
    retail_type                   varchar(50)                     null comment '零售业态 (自动带出)',
    customer_code                 varchar(64)                     null comment '上级客户编码 (自动带出)',
    customer_name                 varchar(200)                    null comment '上级客户名称 (自动带出)',
    abate_status                  varchar(20) default 'EFFECTIVE' null comment '失效状态: EFFECTIVE/INVALID',
    delete_flag                   int         default 0           not null comment '逻辑删除标志',
    version_num                   int         default 1           not null comment '乐观锁版本号',
    created_by                    bigint                          not null comment '创建人',
    creation_date                 datetime                        not null comment '创建时间',
    last_updated_by               bigint                          not null comment '最后更新人',
    last_update_date              datetime                        not null comment '最后更新时间',
    last_update_login             bigint                          null comment '最后更新登录ID'
)
    comment '门店商品价格段门店行表'
    charset = utf8mb4;

create index idx_product_line_id
    on ims_product_price_store_segment_store_line (store_segment_product_line_id);

create index idx_store_code
    on ims_product_price_store_segment_store_line (store_code);

create index idx_store_segment_header_id
    on ims_product_price_store_segment_store_line (store_segment_header_id);
    
create table ims_product_price_store_segment
(
    segment_price_id    bigint                          not null comment '主键ID（雪花算法）'
        primary key,
    product_sap_id      varchar(64)                     not null comment '产品SAP ID',
    product_name        varchar(200)                    null comment '产品名称',
    product_big_type    varchar(100)                    null comment '产品大类',
    product_middle_type varchar(100)                    null comment '产品中类',
    product_small_type  varchar(100)                    null comment '产品小类',
    store_code          varchar(64)                     not null comment '门店编码',
    store_name          varchar(200)                    null comment '门店名称',
    retail_type         varchar(50)                     null comment '零售业态',
    customer_code       varchar(64)                     null comment '上级客户编码',
    customer_name       varchar(200)                    null comment '上级客户名称',
    start_date          datetime                        not null comment '起始日期（取自申请单）',
    end_date            datetime                        not null comment '终止日期（取自申请单）',
    price_min           decimal(18, 2)                  not null comment '价格下限（元）',
    price_max           decimal(18, 2)                  not null comment '价格上限（元）',
    segment_code        varchar(64)                     null comment '关联申请单号',
    segment_name        varchar(200)                    null comment '单据名称',
    effective_status    varchar(20) default 'EFFECTIVE' not null comment '状态: EFFECTIVE/INVALID',
    delete_flag         int         default 0           not null comment '逻辑删除标志',
    version_num         int         default 1           not null comment '乐观锁版本号',
    created_by          bigint                          not null comment '创建人',
    creation_date       datetime                        not null comment '创建时间',
    last_updated_by     bigint                          not null comment '最后更新人',
    last_update_date    datetime                        not null comment '最后更新时间',
    last_update_login   bigint                          null comment '最后更新登录ID'
)
    comment '门店商品价格段明细表'
    charset = utf8mb4;

create index idx_date_range
    on ims_product_price_store_segment (start_date, end_date);

create index idx_effective_status
    on ims_product_price_store_segment (effective_status);

create index idx_product_sap_id
    on ims_product_price_store_segment (product_sap_id);

create index idx_product_store
    on ims_product_price_store_segment (product_sap_id, store_code);

create index idx_segment_code
    on ims_product_price_store_segment (segment_code);

create index idx_store_code
    on ims_product_price_store_segment (store_code);
```



## 配置esb接口

1.梅西查询门店商品销售名称接口

2.梅西查询门店商品价格段接口