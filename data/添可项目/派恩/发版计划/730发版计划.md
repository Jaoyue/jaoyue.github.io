> HTML 页面: [[page/wiki/data/添可项目/派恩/发版计划/730发版计划.html|打开 HTML 页面]]

### sql
```sql
-- 假故障新分支appkey
INSERT INTO `pyne_agent`.`pn_config` ( `create_by`, `create_time`, `update_by`, `update_time`, `is_deleted`, `version`, `config_key`, `config_value`, `config_desc`) VALUES ('system', '2025-06-10 00:00:00', 'system', '2025-06-10 00:00:00', 0, 0, 'agent_function_ffs2Apikey', 'app-NqgbZFJUq4RQIuDUVtigcjjQ', NULL);
-- 功能说明书字段修改长度
ALTER TABLE `pyne_agent`.`pn_fake_fault_sop_process` 
MODIFY COLUMN `functional_specification_url` text CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NULL COMMENT '功能说明书' AFTER `jira_document_url`;
-- 添可bom数据增加客户类型
 ALTER TABLE `pyne_agent`.`pn_bom_dev_tineco` 
ADD COLUMN `customer_type` varchar(255) NULL COMMENT '客户类型';
 ALTER TABLE `pyne_agent`.`pn_bom_part_tineco` 
ADD COLUMN `customer_type` varchar(255) NULL COMMENT '客户类型';
-- 知识库记录增加操作人
ALTER TABLE `pyne_agent`.`pn_dataset_upload_record` 
ADD COLUMN `operator` varchar(255) NULL COMMENT '操作人'
ADD COLUMN `data_name` varchar(1000) NULL COMMENT '知识名称';
-- 历史任务表加字段
ALTER TABLE `pyne_agent`.`pn_agent_log` 
ADD COLUMN `push_sap_id` varchar(255) NULL DEFAULT NULL COMMENT '推送下游的sapid'
```

### esb接口


### 配置文件
```yaml
gauss:
    security:
        permit-all-urls:
            - /pnProduct/getProduct4SapId
            - /pnObmMroProcess/launchProcess
            - /pnObmMroProcess/launchProcess4Third
            - /pnObmMroProcess/getAgentBomType
            - /pn-api/getRepairAss
            - /pn-api/getDfmeaUrl
```

