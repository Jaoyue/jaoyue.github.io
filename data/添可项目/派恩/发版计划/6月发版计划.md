### 已发功能点
:::warning
1.知识库增加故障分类

2.知识库维修SOP文件类型变更为Excel

3.排障类型拆分

:::

### 630待发功能
:::warning
1.假故障SOP

2.维修SOP

3.舆情处理

4.BOM一键获取

5.可维修性评估增加type(重新生成/更新dfmea内容)

:::

### 加配置
```yaml
    onlineEditUrl: https://t-esb-gateway.tineco.com/restcloud/tinechat-server/v1/js/query/edit/url
    onlineDownloadUrl: https://t-esb-gateway.tineco.com/restcloud/tinechat-server/api/v1/js/download/file
    
    gauss:
    security:
        permit-all-urls:
            - /pnProduct/getProduct4SapId
            - /pnObmMroProcess/launchProcess
            - /pnObmMroProcess/launchProcess4Third
            - /pnObmMroProcess/getAgentBomType
```

### sql脚本
:::warning
INSERT INTO `pyne_agent`.`pn_config` (`id`, `create_by`, `create_time`, `update_by`, `update_time`, `is_deleted`, `version`, `config_key`, `config_value`, `config_desc`) VALUES (20, 'system', '2025-06-03 16:51:40', 'system', '2025-06-03 16:51:40', 0, 0, 'agent_workflow_pdf2excelUrl', '[https://dify-eco-qs.tineco.com/v1/workflows/run',](https://dify-eco-qs.tineco.com/v1/workflows/run',) NULL);

INSERT INTO `pyne_agent`.`pn_config` (`id`, `create_by`, `create_time`, `update_by`, `update_time`, `is_deleted`, `version`, `config_key`, `config_value`, `config_desc`) VALUES (21, 'system', '2025-06-03 16:51:40', 'system', '2025-06-03 16:51:40', 0, 0, 'agent_workflow_pdf2excelApikey', 'app-ajiaNtlrhXeI9JhnW7eMEzno', NULL);

<font style="color:#DF2A3F;">INSERT INTO `pyne_agent`.`pn_config` (`create_by`, `create_time`, `update_by`, `update_time`, `is_deleted`, `version`, `config_key`, `config_value`, `config_desc`) VALUES ('system', '2025-06-04 00:00:00', 'system', '2025-06-04 00:00:00', 0, 0, 'agent_function_mroMroApikey', 'app-EeXoWI47eP5Rqz4mZxbeAEpu', NULL);</font>

ALTER TABLE `pyne_agent`.`pn_repair_assessment_process` 

ADD COLUMN `type` varchar(50) NULL COMMENT '类型' AFTER `status`;

<font style="color:#DF2A3F;">ALTER TABLE `pyne_agent`.`pn_obm_mro_process` </font>

<font style="color:#DF2A3F;">ADD COLUMN `llm` varchar(50) NULL COMMENT '模型编码' AFTER `end_time`;</font>

<font style="color:#DF2A3F;">ALTER TABLE `pyne_agent`.`pn_obm_mro_process` </font>

<font style="color:#DF2A3F;">CHANGE COLUMN `product_code` `agent_obm_type` text CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NULL DEFAULT NULL COMMENT 'agent排障类型' AFTER `product_id`;</font>

CREATE TABLE `pyne_agent`.`pn_obm_type_rel`  (

  `id` bigint NOT NULL AUTO_INCREMENT COMMENT 'id',

  `create_by` varchar(50) CHARACTER SET utf8mb3 COLLATE utf8mb3_bin NULL DEFAULT NULL COMMENT '创建人',

  `create_time` datetime NULL DEFAULT NULL COMMENT '创建时间',

  `update_by` varchar(50) CHARACTER SET utf8mb3 COLLATE utf8mb3_bin NULL DEFAULT NULL COMMENT '修改人',

  `update_time` datetime NULL DEFAULT NULL COMMENT '修改时间',

  `is_deleted` tinyint NOT NULL DEFAULT 0 COMMENT '禁用启用 0：启用  1禁用',

  `version` int NULL DEFAULT NULL COMMENT '版本号',

  `fault_class` varchar(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NULL DEFAULT NULL COMMENT '故障类型',

  `obm_process_id` bigint NULL DEFAULT NULL COMMENT '排障流程id',

  PRIMARY KEY (`id`) USING BTREE

) ENGINE = InnoDB CHARACTER SET = utf8mb4 COLLATE = utf8mb4_bin ROW_FORMAT = Dynamic;

ALTER TABLE `pyne_agent`.`pn_obm_type_rel` 

ADD INDEX `idx_fault_class`(`fault_class`) USING BTREE,

ADD INDEX `idx_obm_process_id`(`obm_process_id`) USING BTREE;

:::

```sql
CREATE TABLE `pyne_agent`.`pn_bom_dev_ecovacs`  (
  `a_id` bigint unsigned NOT NULL AUTO_INCREMENT COMMENT 'id',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '创建时间',
  `client` varchar(255) NULL DEFAULT NULL COMMENT '业态组',
  `struc_grade` varchar(255) NULL DEFAULT NULL COMMENT '结构级别',
  `target_type_specifier` varchar(255) NULL DEFAULT NULL COMMENT '对象类型指示符',
  `no` varchar(255) NULL DEFAULT NULL COMMENT '编号',
  `code` varchar(255) NULL DEFAULT NULL COMMENT '编码',
  `name` varchar(255) NULL DEFAULT NULL COMMENT '名称',
  `classify` varchar(255) NULL DEFAULT NULL COMMENT '分类',
  `qty` varchar(20) NULL DEFAULT NULL COMMENT '数量',
  `unit` varchar(255) NULL DEFAULT NULL COMMENT '单位',
  `nw` varchar(255) NULL DEFAULT NULL COMMENT '净重',
  `status` varchar(255) NULL DEFAULT NULL COMMENT '状态',
  `color` varchar(255) NULL DEFAULT NULL COMMENT '颜色',
  `auth_model` varchar(255) NULL DEFAULT NULL COMMENT '认证型号',
  `mkt_name` varchar(255) NULL DEFAULT NULL COMMENT '市场名',
  `ean_upc` varchar(255) NULL DEFAULT NULL COMMENT 'EAN/UPC',
  `country_chl` varchar(255) NULL DEFAULT NULL COMMENT '国家及渠道',
  `etl_time` datetime NULL DEFAULT NULL COMMENT '更新时间',
  `father_lvl_material_no` varchar(255) NULL DEFAULT NULL COMMENT '父级料号',
  `fp_material_no` varchar(255) NULL DEFAULT NULL COMMENT '成品料号',
  `purchase_type` varchar(255) NULL DEFAULT NULL COMMENT '采购类型',
  `special_purchase_type` varchar(255) NULL DEFAULT NULL COMMENT '特殊采购类型',
  PRIMARY KEY (`a_id`) USING BTREE
) COMMENT '科沃斯研发BOM';
CREATE TABLE `pyne_agent`.`pn_bom_dev_tineco`  (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT COMMENT 'id',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '创建时间',
  `product_sap_id` varchar(50) NULL DEFAULT NULL COMMENT '根节点(产品编码)',
  `level_number` varchar(50) NULL DEFAULT NULL COMMENT '结构层级',
  `upper_cpnt_code` varchar(50) NULL DEFAULT NULL COMMENT '父节点(上层物料编码)',
  `cpnt_code` varchar(50) NULL DEFAULT NULL COMMENT '自身节点(物料编码)',
  `cpnt_desc` varchar(255) NULL DEFAULT NULL COMMENT '物料名称',
  `material_group` varchar(255) NULL DEFAULT NULL COMMENT '分类',
  `cpnt_qty` varchar(20) NULL DEFAULT NULL COMMENT '数量',
  `cpnt_unit_meas` varchar(50) NULL DEFAULT NULL COMMENT '单位',
  `net_weight` varchar(255) NULL DEFAULT NULL COMMENT '净重',
  `status` varchar(255) NULL DEFAULT NULL COMMENT '状态',
  `color` varchar(255) NULL DEFAULT NULL COMMENT '颜色',
  `certify_model` varchar(1000) NULL DEFAULT NULL COMMENT '认证型号(市场型号)',
  `market_region` varchar(255) NULL DEFAULT NULL COMMENT '上市地区(销售市场)',
  `product_name` varchar(255) NULL DEFAULT NULL COMMENT '产品名称',
  `six_nine_code` varchar(255) NULL DEFAULT NULL COMMENT '69码',
  `product_category` varchar(255) NULL DEFAULT NULL COMMENT '产品分类',
  `product_name_en` varchar(255) NULL DEFAULT NULL COMMENT '英文名称',
  PRIMARY KEY (`id`) USING BTREE
) COMMENT '添可研发BOM';
CREATE TABLE `pyne_agent`.`pn_bom_part_ecovacs`  (
  `a_id` bigint unsigned NOT NULL AUTO_INCREMENT COMMENT 'id',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '创建时间',
  `client` varchar(255) NULL DEFAULT NULL COMMENT '业态组',
  `target_type_specifier` varchar(255) NULL DEFAULT NULL COMMENT '对象类型指示符',
  `code` varchar(255) NULL DEFAULT NULL COMMENT '编码',
  `name` varchar(255) NULL DEFAULT NULL COMMENT '名称',
  `classify` varchar(255) NULL DEFAULT NULL COMMENT '分类',
  `qty` varchar(20) NULL DEFAULT NULL COMMENT '数量',
  `unit` varchar(255) NULL DEFAULT NULL COMMENT '单位',
  `en_name` varchar(255) NULL DEFAULT NULL COMMENT '英文名称',
  `effect_mkt` varchar(255) NULL DEFAULT NULL COMMENT '有效市场',
  `spares_attr` varchar(255) NULL DEFAULT NULL COMMENT '备件属性',
  `repair_grade` varchar(255) NULL DEFAULT NULL COMMENT '维修等级',
  `packing_qty` varchar(255) NULL DEFAULT NULL COMMENT '装箱数量',
  `status` varchar(255) NULL DEFAULT NULL COMMENT '状态',
  `etl_time` datetime NULL DEFAULT NULL COMMENT '更新时间',
  PRIMARY KEY (`a_id`) USING BTREE
) COMMENT '科沃斯散备件BOM';
CREATE TABLE `pyne_agent`.`pn_bom_part_tineco`  (
  `id` bigint unsigned NOT NULL AUTO_INCREMENT COMMENT 'id',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '创建时间',
  `product_name` varchar(255) NULL DEFAULT NULL COMMENT '散备件名称',
  `product_sap_id` varchar(255) NULL DEFAULT NULL COMMENT '散备件编码',
  `part_name` varchar(255) NULL DEFAULT NULL COMMENT '零件名称',
  `part_code` varchar(255) NULL DEFAULT NULL COMMENT '零件编码',
  `cpnt_qty` varchar(20) NULL DEFAULT NULL COMMENT '数量',
  `cpnt_unit_meas` varchar(50) NULL DEFAULT NULL COMMENT '单位',
  `material_group` varchar(255) NULL DEFAULT NULL COMMENT '分类(物料组)',
  `status` varchar(255) NULL DEFAULT NULL COMMENT '状态',
  `product_name_en` varchar(255) NULL DEFAULT NULL COMMENT '英文名称',
  PRIMARY KEY (`id`) USING BTREE
) COMMENT '添可散备件BOM';
```

