1.知识库维修SOP, pdf文件更换位Excel文件

2.排障拆分为3个大模型

3.知识库增加故障分类



:::warning
ALTER TABLE `pyne_agent`.`pn_obm_mro_process` 

CHANGE COLUMN `product_code` `agent_obm_type` text CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NULL DEFAULT NULL COMMENT 'agent排障类型' AFTER `product_id`;

ALTER TABLE `pyne_agent`.`pn_obm_mro_process` 

ADD COLUMN `llm` varchar(50) CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NULL DEFAULT NULL COMMENT '模型编码' AFTER `end_time`;

:::

