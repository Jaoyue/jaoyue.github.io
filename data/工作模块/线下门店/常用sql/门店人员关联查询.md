```plsql
select s.name `门店名称`,s.store_no `门店编号`,a.staffs 分部经理,b.staffs 渠道经理,c.staffs 零售经理 from 
sp_store_info s left join
(
select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from (
select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id
left join sp_biz_staff s on p.staff_id = s.id 
where staff_status = 'ENABLED' and s.position = 'REGION_MANAGER') t
group by store_no) a on s.store_no =a.store_no
left join(
select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from (
select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id
left join sp_biz_staff s on p.staff_id = s.id 
where staff_status = 'ENABLED' and s.position = 'BIZ_MANAGER') t
group by store_no) b on s.store_no = b.store_no
left join(
select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from (
select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id
left join sp_biz_staff s on p.staff_id = s.id 
where staff_status = 'ENABLED' and s.position = 'BIZ_SUPERVISOR') t
group by store_no) c on s.store_no = c.store_no
```

```plsql
select s.name `门店名称`,s.store_no `门店编号`,a.staffs 分部经理,b.staffs 渠道经理,c.staffs 零售经理 from  sp_store_info s left join ( select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from ( select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id left join sp_biz_staff s on p.staff_id = s.id  where staff_status = 'ENABLED' and s.position = 'REGION_MANAGER') t group by store_no) a on s.store_no =a.store_no left join( select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from ( select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id left join sp_biz_staff s on p.staff_id = s.id  where staff_status = 'ENABLED' and s.position = 'BIZ_MANAGER') t group by store_no) b on s.store_no = b.store_no left join( select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from ( select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id left join sp_biz_staff s on p.staff_id = s.id  where staff_status = 'ENABLED' and s.position = 'BIZ_SUPERVISOR') t group by store_no) c on s.store_no = c.store_no







select distinct store_name,store_no,staff_name,if(position = 'REGION_MANAGER',1,null) 分部经理,if(position = 'BIZ_MANAGER',1,null) 业务经理,if(position = 'BIZ_SUPERVISOR',1,null) 终端督导 from (
select i.name store_name,i.store_no,s.name staff_name,s.position from sp_biz_staff_store_permission p
join sp_store_info i on i.id = p.staff_id
join sp_biz_staff s on p.staff_id = s.id and s.staff_status = 'ENABLED'
where s.position in ('REGION_MANAGER','BIZ_MANAGER','BIZ_SUPERVISOR')
) t 

select store_name,store_no,position,GROUP_CONCAT(staff_name) from(
select distinct * from(
select i.name store_name,i.store_no,s.name staff_name,s.position from sp_biz_staff_store_permission p
join sp_store_info i on i.id = p.staff_id
join sp_biz_staff s on p.staff_id = s.id and s.staff_status = 'ENABLED'
where s.position in ('REGION_MANAGER','BIZ_MANAGER','BIZ_SUPERVISOR') ) m) t
group by store_no,position




select * from sp_sales_order_item where product_name like '%饮万%'
select * from sp_biz_staff i left join sp_biz_staff_store_permission p on i.id = p.store_id



select s.name `门店名称`,s.store_no `门店编号`,a.staffs 分部经理,b.staffs 渠道经理,c.staffs 零售经理 from 
sp_store_info s left join
(
select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from (
select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id
left join sp_biz_staff s on p.staff_id = s.id 
where staff_status = 'ENABLED' and s.position = 'REGION_MANAGER') t
group by store_no) a on s.store_no =a.store_no
left join(
select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from (
select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id
left join sp_biz_staff s on p.staff_id = s.id 
where staff_status = 'ENABLED' and s.position = 'BIZ_MANAGER') t
group by store_no) b on s.store_no = b.store_no
left join(
select store_name,store_no,position,GROUP_CONCAT(staff_name) staffs from (
select distinct i.name store_name,i.store_no,s.name staff_name,s.position from sp_store_info i left join sp_biz_staff_store_permission p on i.id = p.store_id
left join sp_biz_staff s on p.staff_id = s.id 
where staff_status = 'ENABLED' and s.position = 'BIZ_SUPERVISOR') t
group by store_no) c on s.store_no = c.store_no

HAVING store_no = '9004346'
```

