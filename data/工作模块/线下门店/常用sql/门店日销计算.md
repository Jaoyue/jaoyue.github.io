> HTML 页面: [[page/wiki/data/工作模块/线下门店/常用sql/门店日销计算.html|打开 HTML 页面]]

#### 20240715最新逻辑_产品销售统计表补数据
```plsql
insert into sp_store_product_stat(`stat_time`, `store_id`, `material_no`, `product_classification`, `sales_count`, `sales_amount`, `workspace_type`, `order_type`)
select stat_time,store_id,material_no,product_classification,sum(count) count,sum(amount) amount,workspace_type,order_type from(
select o.store_id,i.material_no,i.product_classification,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as amount ,s.workspace_type, o.order_type,UNIX_TIMESTAMP(FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'))*1000 stat_time from sp_sales_order o left JOIN sp_sales_order_item i on i.sales_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id   where o.order_time BETWEEN 1557676800000 and 1721231999000 and o.`status`='FINISHED' and i.is_gift='N'   group by FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.product_classification,i.material_no,o.order_type
union all
select o.store_id,i.material_no,i.product_classification,-sum(i.count) count,-CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as amount ,s.workspace_type,so.order_type,UNIX_TIMESTAMP(FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'))*1000 stat_time from sp_refund_order o left JOIN sp_refund_order_item i on i.refund_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id left join sp_sales_order so on o.sales_order_no = so.order_no   where o.order_time BETWEEN 1557676800000 and 1721231999000 and o.`status`='FINISHED' and i.is_gift='N'   group by FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.product_classification,i.material_no,so.order_type
)t GROUP BY stat_time,store_id,product_classification,material_no,order_type

delete from sp_store_product_stat

select stat_time,store_id,material_no,product_classification,sum(count) count,sum(amount) amount,workspace_type,order_type from(
select o.store_id,i.material_no,i.product_classification,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as amount ,s.workspace_type, o.order_type,UNIX_TIMESTAMP(FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'))*1000 stat_time from sp_sales_order o left JOIN sp_sales_order_item i on i.sales_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id   where o.order_time BETWEEN 1721059200000 and 1721231999000 and o.`status`='FINISHED' and i.is_gift='N'   group by FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.product_classification,i.material_no,o.order_type
union all
select o.store_id,i.material_no,i.product_classification,-sum(i.count) count,-CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as amount ,s.workspace_type,so.order_type,UNIX_TIMESTAMP(FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'))*1000 stat_time from sp_refund_order o left JOIN sp_refund_order_item i on i.refund_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id left join sp_sales_order so on o.sales_order_no = so.order_no   where o.order_time BETWEEN 1721059200000 and 1721231999000 and o.`status`='FINISHED' and i.is_gift='N'   group by FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.product_classification,i.material_no,so.order_type 
)t GROUP BY stat_time,store_id,product_classification,material_no,order_type HAVING stat_time = 1721059200000
```

```plsql
select sum(lineAmount) from (
select o.sales_region,i.product_classification,sum(i.count),CAST(sum(i.count *i.sale_price_down) AS  DECIMAL(15,3)) as lineAmount ,s.province_id,s.city_id,o.store_id ,sum(i.count*i.product_discount_ratio),CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as averageAmount ,s.workspace_type from sp_refund_order o left JOIN sp_refund_order_item i on i.refund_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id   where o.order_time BETWEEN 1658332800000 and 1658419200000 and o.`status`='FINISHED' and i.is_gift='N'  and refund_reason_type='input_mistake'  group by o.sales_region,o.store_id,i.product_classification) t

select sum(averageAmount) from(
select o.sales_region,i.product_classification,sum(i.count),CAST(sum(i.count *i.sale_price_down) AS  DECIMAL(15,3)) as lineAmount ,s.province_id,s.city_id,o.store_id ,sum(i.count*i.product_discount_ratio),CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as averageAmount ,s.workspace_type from sp_refund_order o left JOIN sp_refund_order_item i on i.refund_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id   where o.order_time BETWEEN 1658332800000 and 1658419200000 and o.`status`='FINISHED' and i.is_gift='N'   group by o.sales_region,o.store_id,i.product_classification) t

select sum(averageAmount) from(
select o.sales_region,i.product_classification,sum(i.count),CAST(sum(i.count *i.sale_price_down) AS  DECIMAL(15,3)) as lineAmount ,s.province_id,s.city_id,o.store_id ,sum(i.count*i.product_discount_ratio),CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) as averageAmount ,s.workspace_type from sp_sales_order o left JOIN sp_sales_order_item i on i.sales_order_no=o.order_no   left JOIN sp_store_info s on o.store_id=s.id   where o.order_time BETWEEN 1657728000000 and 1657814399999 and o.`status`='FINISHED' and i.is_gift='N'   group by o.sales_region,o.store_id,i.product_classification
) t


select 1657728000000 stat_time,store_id,a.material_no,p.product_classification,sales_count,sales_amount,'TASK_SYSTEM' creater,1657728000000 create_time,'TASK_SYSTEM' modifier,1657728000000 modify_time,0 version,s.workspace_type from 
(select 1657728000000 stat_time,t.store_id store_id,t.material_no material_no,sum(t.count) sales_count,sum(t.total) sales_amount from 
(
select o.store_id,i.material_no,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_sales_order o
right join sp_sales_order_item i on o.order_no = i.sales_order_no
where o.order_time >= 1658332800000 and o.order_time < 1658419200000
and o.`status`='FINISHED' and i.is_gift='N' 
GROUP BY o.store_id,i.material_no
union all
select o.store_id,i.material_no,sum(0-i.count) count,sum(0-i.total) total from sp_refund_order o
right join sp_refund_order_item i on o.order_no = i.refund_order_no
where o.order_time >= 1658332800000 and o.order_time < 1658419200000 and
o.`status`='FINISHED' and i.is_gift='N'  
GROUP BY o.store_id,i.material_no
) t group by t.store_id,t.material_no
) a
left join sp_product p on a.material_no = p.material_no
left join sp_store_info s on a.store_id = s.id

select sum(total) from(
select sum(total) from(
select o.store_id,i.material_no,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_sales_order o
right join sp_sales_order_item i on o.order_no = i.sales_order_no
where o.order_time >= 1658332800000 and o.order_time < 1658419200000
and o.`status`='FINISHED' and i.is_gift='N' 
GROUP BY o.store_id,i.material_no
) n
union all
select sum(total) from(
select o.store_id,i.material_no,sum(0-i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_refund_order o
right join sp_refund_order_item i on o.order_no = i.refund_order_no
where o.order_time >= 1658332800000 and o.order_time < 1658419200000 and
o.`status`='FINISHED' and i.is_gift='N'  
GROUP BY o.store_id,i.material_no
) m

select sum(total) from(
select o.store_id,i.material_no,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_sales_order o
right join sp_sales_order_item i on o.order_no = i.sales_order_no
where o.order_time >= 1658332800000 and o.order_time < 1658419200000
and o.`status`='FINISHED' and i.is_gift='N' 
GROUP BY o.store_id,i.material_no
union all
select o.store_id,i.material_no,sum(0-i.count) count,0-CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_refund_order o
right join sp_refund_order_item i on o.order_no = i.refund_order_no
where o.order_time >= 1658332800000 and o.order_time < 1658419200000 and
o.`status`='FINISHED' and i.is_gift='N'  
GROUP BY o.store_id,i.material_no) m

select sum(sales_amount) from sp_store_product_stat where stat_time = 1658332800000; 












--  20230605精修版
select UNIX_TIMESTAMP(t.date)*1000 stat_time,t.store_id store_id,t.material_no material_no,sum(t.count) sales_count,sum(t.total) sales_amount from 
(
select FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d') date,o.store_id,i.material_no,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_sales_order o
right join sp_sales_order_item i on o.order_no = i.sales_order_no
where o.order_time >= 1653494400000 and o.order_time < 1672502400000
and o.`status`='FINISHED' and i.is_gift='N' 
GROUP BY FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.material_no
union all
select FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d') date,o.store_id,i.material_no,sum(0-i.count) count,sum(0-i.total) total from sp_refund_order o
right join sp_refund_order_item i on o.order_no = i.refund_order_no
where o.order_time >= 1653494400000 and o.order_time < 1672502400000 and
o.`status`='FINISHED' and i.is_gift='N'  
GROUP BY FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.material_no
) t group by t.date,t.store_id,t.material_no



















-- 20230613补数据sql

insert into sp_store_product_stat(stat_time,store_id,material_no,product_classification,sales_count,sales_amount,workspace_type,create_time,modify_time,version)
select r.stat_time stat_time,r.store_id store_id,r.material_no material_no,p.product_classification product_classification,r.sales_count sales_count,r.sales_amount sales_amount,i.workspace_type workspace_type,1686636132088 create_time,1686636132088 modify_time,0 version
from (
select UNIX_TIMESTAMP(t.date)*1000 stat_time,t.store_id store_id,t.material_no material_no,sum(t.count) sales_count,sum(t.total) sales_amount from 
(
select FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d') date,o.store_id,i.material_no,sum(i.count) count,CAST(sum(i.count *i.average_price) AS  DECIMAL(15,3)) total from sp_sales_order o
right join sp_sales_order_item i on o.order_no = i.sales_order_no
where o.order_time >= 1678809600000
and o.`status`='FINISHED' and i.is_gift='N' 
GROUP BY FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.material_no
union all
select FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d') date,o.store_id,i.material_no,sum(0-i.count) count,sum(0-i.total) total from sp_refund_order o
right join sp_refund_order_item i on o.order_no = i.refund_order_no
where o.order_time >= 1678809600000  and
o.`status`='FINISHED' and i.is_gift='N'  
GROUP BY FROM_UNIXTIME(o.order_time/1000,'%Y-%m-%d'),o.store_id,i.material_no
) t group by t.date,t.store_id,t.material_no 
) r join sp_product p on r.material_no = p.material_no
join sp_store_info i on r.store_id = i.id
order by stat_time

```

