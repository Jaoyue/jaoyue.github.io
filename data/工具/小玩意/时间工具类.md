> HTML 页面: [[page/wiki/data/工具/小玩意/时间工具类.html|打开 HTML 页面]]

```java
package com.ecovacs.common.util;

import com.ecovacs.common.enums.WeekDayEnum;
import com.ecovacs.core.exception.EcovacsValidateException;
import org.apache.commons.lang.ArrayUtils;
import org.apache.commons.lang.StringUtils;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.sql.Timestamp;
import java.text.DateFormat;
import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.*;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class DateUtil {
    public static final int DAY_SECONDS = 24 * 60 * 60;
    public static final int FIVE_MINUTE_SECONDS = 5 * 60;
    public static final long YEAR_MILLION_SECONDS = 365 * 24 * 60 * 60 * 1000L;
    public static final long MONTH_MILLION_SECONDS = 30 * 24 * 60 * 60 * 1000L;
    public static final long WEEK_MILLION_SECONDS = 7 * 24 * 60 * 60 * 1000L;
    public static final long DAY_MILLION_SECONDS = 24 * 60 * 60 * 1000L;
    public static final long HOUR_MILLION_SECONDS = 60 * 60 * 1000L;
    public static final long HAIF_AN_HOUR_MILLION_SECONDS = 30 * 60 * 1000L;
    public static final long MINUTE_MILLION_SECONDS = 60 * 1000L;
    public static final long THREE_MINUTE_MILLION_SECONDS = 3 * 60 * 1000L;
    public static final long FIVE_MINUTE_MILLION_SECONDS = 5 * 60 * 1000L;
    public static final long FIVE_SECONDS_MILLION_SECONDS = 5 * 1000L;
    public static final long FOUR_MINUTE_MILLION_SECONDS = 4 * 60 * 1000L;
    public static final long TEN_MINUTE_MILLION_SECONDS = 10 * 60 * 1000L;
    public static final long MILLOIN_SECONDS = 1000L;
    public static final String DATE_STRING_FORMAT_ALL = "yyyy-MM-dd HH:mm:ss";
    public static final String DATE_STRING_FORMAT_ALL_MILLISECOND = "yyyy-MM-dd HH:mm:ss.SSS";
    public static final String DATE_STRING_FORMAT_ALL_2 = "yyyy/MM/dd HH:mm:ss";
    public static final String DATE_STRING_FORMAT_ALL_3 = "yyyy/MM/dd HH:mm";
    public static final String DATE_STRING_FORMAT_DEFAULT = "yyyy-MM-dd HH:mm";
    public static final String DATE_STRING_FORMAT_CN = "yyyy年MM月dd日";
    public static final String DATE_STRING_FORMAT_WEEK_CN = "yyyy/MM/dd E";
    public static final String YEAR_MONTH_DATE = "yyyy-MM-dd";
    public static final String YEAR_MONTH = "yyyy-MM";
    public static final String YEAR = "yyyy";
    public static final String YEAR_MONTH_1 = "yyyyMM";
    public static final String YEAR_MONTH_DATE_2 = "yyyy/MM/dd";
    public static final String YEAR_MONTH_DATE_6 = "yyyy/M/d";
    public static final String MONTH_1 = "M";
    public static final String YEAR_MONTH_DATE_7 = "yyyyM";
    public static final String DATE_1 = "d";
    public static final String DATE_STRING_FORMAT_ALL2 = "yyyy/MM/dd HH:mm:ss";
    public static final String DATE_STRING_FORMAT_ALL3 = "yyyyMMddHHmmss";
    public static final String DATE_STRING_FORMAT_ALL4 = "MM/dd/yyyy HH:mm:ss";
    public static final String YEAR_MONTH_DATE_3 = "yyyyMMdd";
    public static final String YEAR_MONTH_DATE_4 = "yy/MM/dd";
    public static final String MONTH_DATE = "MM-dd";
    public static final String MONTH_DATE_1 = "MM/dd";
    public static final String DATE_FORMAT_TIME = "HH:mm";
    public static final String YEAR_MONTH_DATE_5 = "yyMMdd";
    public static final String DATE_STRING_FORMAT_DAY = "yyyy-MM-dd";
    public static final String DATE_STRING_FORMAT_DAY_HOUR_MIN = "yyyy-MM-dd HH:mm";
    public static final String DATE_STRING_FORMAT_DAY2 = "yyyy.MM.dd";
    public static final String DATE_STRING_FORMAT_DAY3 = "yyyyMMdd";
    public static final String DATE_STRING_FORMAT_CN2 = "yyyy年MM月";
    public static final String MONTH_DATE_CN = "MM月dd日";
    public static final String DATE_STRING_FORMAT_CN3 = "yyyy年";
    public static final String TIME = "HH:mm:ss";
    public static final String DEFAULT = "yyyy-MM-dd";
    public static final String STANDARD = "yyyy-MM-dd HH:mm";
    public static final String ALL = "yyyy-MM-dd HH:mm:ss";
    public static final String ALL_INCLUDE_MILLISECOND = "yyyy-MM-dd HH:mm:ss:SSS";
    public static final String ALL_SHORT = "yyyyMMddHHmmss";
    public static final String STANDARD_CN = "yyyy年MM月dd日 HH时mm分";
    public static final String DATE_STRING_FORMAT_DAY_HOUR_MIN_CN = "yyyy年MM月dd日 HH时mm分";
    public static final String DATE_STRING_FORMAT_MON_DAY_HOUR_MIN_CN = "MM月dd日 HH时mm分";
    private static final Logger LOG = LoggerFactory.getLogger(DateUtil.class);
    private static final String SIMPLE_CN = "yyyy年MM月dd日 HH时mm分ss秒";

    public static final String DATE_2_CRON = "ss mm HH dd MM ? yyyy;";

    private final static String[] CONSTELLATION_ARR = new String[]{"摩羯座", "水瓶座", "双鱼座", "白羊座", "金牛座",
            "双子座", "巨蟹座", "狮子座", "处女座", "天秤座", "天蝎座", "射手座", "摩羯座"};
    //星座对应的每个月的截止日期
    private final static int[] CONSTELLATION_DAY_ARR = new int[]{20, 19, 21, 20, 21, 22, 23, 23, 23, 24, 23, 22};


    /**
     * 获取当前的时间
     *
     * @return
     */
    public static Long getNowLong() {
        Long result = System.currentTimeMillis();
        return result;
     /*   Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(System.currentTimeMillis());
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        calendar.set(Calendar.DATE, 28);
        calendar.set(Calendar.MONTH, calendar.get(Calendar.MONTH)-1);
        return calendar.getTimeInMillis();*/

    }

    // 将时间戳转成字符串
    public static String getDateToString(long time) {
        Date d = new Date(time);
        SimpleDateFormat sf = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return sf.format(d);
    }

    /**
     * Java通过日期计算星座
     *
     * @param date Date
     * @return 星座中文
     */
    public static String getConstellation(Date date) {
        if (date == null) {
            return null;
        }
        Calendar time = Calendar.getInstance();
        time.setTime(date);
        int month = time.get(Calendar.MONTH) + 1;
        int day = time.get(Calendar.DAY_OF_MONTH);
        if (day < CONSTELLATION_DAY_ARR[month - 1]) {
            month = month - 1;
        }
        return CONSTELLATION_ARR[month];
    }

    /**
     * 将数字类型时间转换为时间字符串
     *
     * @param dateNumber
     * @param formatStr
     * @return
     */
    public static String dateLongToStr(Long dateNumber, String formatStr) {
        if (dateNumber == null || dateNumber.equals(0L) || formatStr == null || formatStr.length() == 0) {
            return "";
        }
        Date date = new Date(dateNumber);
        SimpleDateFormat sdf = new SimpleDateFormat(formatStr);
        return sdf.format(date);
    }

    public static String dateLongToStr(Long dateNumber) {
        Date date = new Date(dateNumber);
        SimpleDateFormat sdf = new SimpleDateFormat(DATE_STRING_FORMAT_DEFAULT);
        return sdf.format(date);
    }

    public static String dateToStr(Date date) {
        if (date == null) {
            return "";
        }
        SimpleDateFormat sdf = new SimpleDateFormat(DATE_STRING_FORMAT_DEFAULT);
        return sdf.format(date);
    }

    /**
     * 转化时间从长整形为指定格式日期
     *
     * @param format
     * @param time
     * @return
     */
    public static String convertDateLongToDateString(String format, Long time) {
        if (time == null) {
            return "";
        }
        SimpleDateFormat df = new SimpleDateFormat(format);
        Timestamp now = new Timestamp(time);
        return df.format(now);
    }

    /**
     * 转化时间从指定格式日期为长整形
     *
     * @param format
     * @param time
     * @return
     */
    public static Long convertDateStringToDateLong(String format, String time) {
        if (time == null || time.trim().equals("")) {
            return null;
        }
        SimpleDateFormat fmt = new SimpleDateFormat(format);
        Date d = null;
        try {
            d = fmt.parse(time);
        } catch (Exception e) {
            LOG.error("convertDateStringToDateLong EcovacsValidateException error!", e);
        }
        return d == null ? null : d.getTime();
    }

    /**
     * 转化时间从指定格式日期为长整形(当天的最后一天)
     *
     * @param format
     * @param time
     * @return
     */
    public static Long convertDateStringToDateLastLong(String format, String time) {
        if (time == null || time.trim().equals("")) {
            return null;
        }
        Long result = null;
        SimpleDateFormat fmt = new SimpleDateFormat(format);
        Date d = null;
        try {
            d = fmt.parse(time);
            String todayLast = dateToStr(d).substring(0, 10) + " 23:59:59";
            Date dd = convertDateStringToDate(DATE_STRING_FORMAT_ALL, todayLast);
            result = dd.getTime();
            //获取当天的最后毫秒数
        } catch (Exception e) {
            LOG.error("convertDateStringToDateLong EcovacsValidateException error!", e);
        }
        return result;
    }

    /**
     * 转化时间从指定格式日期为长整形 抛出异常
     *
     * @param format
     * @param time
     * @return
     */
    public static Long convertStringToLongThrowE(String format, String time) throws ParseException {
        if (time == null || time.trim().equals("")) {
            return null;
        }
        SimpleDateFormat fmt = new SimpleDateFormat(format);
        Date d = fmt.parse(time);
        return d == null ? null : d.getTime();
    }

    public static Long convertDateStringToDateLong(String format, String time, Locale locale) {
        if (time == null || time.trim().equals("")) {
            return null;
        }
        SimpleDateFormat fmt = new SimpleDateFormat(format, locale);
        Date d = null;
        try {
            d = fmt.parse(time);
            return d.getTime();
        } catch (Exception e) {
            LOG.error("convertDateStringToDateLong EcovacsValidateException error!", e);
        }
        return null;
    }

    /**
     * 转化时间从指定格式日期为长整形
     *
     * @param format
     * @param time
     * @return
     */
    public static Date convertDateStringToDate(String format, String time) {
        if (time == null || time.trim().equals("")) {
            return null;
        }
        SimpleDateFormat fmt = new SimpleDateFormat(format);
        Date d = null;
        try {
            d = fmt.parse(time);
        } catch (Exception e) {
            LOG.error("convertDateStringToDate EcovacsValidateException error!", e);
        }
        return d;
    }

    public static String convertDateLong2SimpleCNFormat(Long time) {
        if (time == null)
            return null;
        return new SimpleDateFormat(SIMPLE_CN).format(new Date(time));
    }

    public static String fullFormat(Date date) {
        if (date == null) {
            return "";
        }
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return format.format(date);
    }

    /**
     * @param formatString
     * @param date
     * @return
     */
    public static String format(String formatString, Date date) {
        if (date == null) {
            return "";
        }
        DateFormat format = new SimpleDateFormat(formatString);
        return format.format(date);
    }

    public static String getLinuxTime() {
        DateFormat format = new SimpleDateFormat("yyyy_MM_dd_HH_mm_ss");
        String r = format.format(new Date());
        return r;
    }

    // 段时间
    public static String toPeriodTime(long m) {
        StringBuffer sb = new StringBuffer();
        long years = m / YEAR_MILLION_SECONDS;
        m = m - years * YEAR_MILLION_SECONDS;
        long months = m / MONTH_MILLION_SECONDS;
        m = m - months * MONTH_MILLION_SECONDS;
        long days = m / DAY_MILLION_SECONDS;
        m = m - days * DAY_MILLION_SECONDS;
        long hours = m / HOUR_MILLION_SECONDS;
        m = m - hours * HOUR_MILLION_SECONDS;
        long minutes = m / MINUTE_MILLION_SECONDS;
        if (0 != years) {
            sb.append(String.valueOf(years));
            sb.append("年");
        }
        if (0 != months) {
            sb.append(String.valueOf(months));
            sb.append("月");
        }
        if (0 != days) {
            sb.append(String.valueOf(days));
            sb.append("天");
        }
        if (0 != hours) {
            sb.append(String.valueOf(hours));
            sb.append("时");
        }
        if (0 != minutes) {
            sb.append(String.valueOf(minutes));
            sb.append("分");
        }
        return sb.toString();
    }

    //日期转换
    public static String convertDateLongToString(Long time) {
        if (time == null) {
            return "";
        }
        Date d = new Date(time);
        SimpleDateFormat sdf = new SimpleDateFormat("yyyy.MM.dd");
        return sdf.format(d);
    }

    public static String convertDateLongToString(Long time, String format) {
        if (time == null) {
            return "";
        }
        Date d = new Date(time);
        SimpleDateFormat sdf = new SimpleDateFormat(format);
        return sdf.format(d);
    }

    public static String convertDateLongToString(Long time, String format, Locale locale) {
        if (time == null) {
            return "";
        }
        Date d = new Date(time);
        if (locale == null) {
            locale = Locale.CHINA;
        }
        SimpleDateFormat sdf = new SimpleDateFormat(format, locale);
        return sdf.format(d);
    }

    /**
     * 得到今天零点零分零秒的Long值
     */
    public static Long getTheDayTime() {
        SimpleDateFormat sdf = new SimpleDateFormat(DATE_STRING_FORMAT_DAY);
        return convertDateStringToDateLong(DATE_STRING_FORMAT_DAY, sdf.format(new Date()));
    }

    /**
     * 得到当前时间，精确到分
     */
    public static Long getTheDayTime2() {
        SimpleDateFormat sdf = new SimpleDateFormat(DATE_STRING_FORMAT_DAY_HOUR_MIN);
        return convertDateStringToDateLong(DATE_STRING_FORMAT_DAY_HOUR_MIN, sdf.format(new Date()));
    }

    /**
     * 得到未来N天
     */
    public static Date getInnerDay(int n) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(System.currentTimeMillis());
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        return calendar.getTime();
    }

    /**
     * 得到未来N天零点零分零秒的Long值
     */
    public static Long getInnerDayTime(int n) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(System.currentTimeMillis());
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        return calendar.getTimeInMillis();
    }

    /**
     * 得到未来N个月的开始时间
     */
    public static Long getInnerMonth(int n) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(System.currentTimeMillis());
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        calendar.set(Calendar.DATE, 1);
        calendar.set(Calendar.MONTH, calendar.get(Calendar.MONTH) + n);
        return calendar.getTimeInMillis();
    }

    /**
     * 得到startTime未来N天零点零分零秒的Long值
     */
    public static Long getInnerDayTime(long standardTime, int differenceDayNum) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(standardTime);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + differenceDayNum);
        return calendar.getTimeInMillis();
    }

    public static Long getInnerDayTime(String standardTime, int differenceDayNum) {
        if (StringUtils.isBlank(standardTime)) return null;
        Long dateLong = convertDateStringToDateLong(DateUtil.DATE_STRING_FORMAT_DAY, standardTime);
        return getInnerDayTime(dateLong, differenceDayNum);
    }

    //找到未来n天的date，如果n是负数就是找前N天
    public static Date getNextNDate(final Date date, int n) {
        if (date == null) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        return calendar.getTime();
    }

    //找到未来n天的time，如果n是负数就是找前N天
    public static Long getNextNTime(final Long time, int n) {
        if (time == null) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(time);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        return calendar.getTimeInMillis();
    }

    /**
     * 获得当前天 前/后 开始时间
     * num  0:当前月的第一天
     * -1:前一个月的第一天
     * 1:后一个月的第一天
     */
    public static Long getNextNStartTime(int n) {
        Calendar calendar = Calendar.getInstance();
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.HOUR, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return calendar.getTimeInMillis();
    }

    /**
     * 获得当前天 前/后 结束时间
     * num  0:当前月的第一天
     * -1:前一个月的第一天
     * 1:后一个月的第一天
     */
    public static Long getNextNEndTime(int n) {
        Calendar calendar = Calendar.getInstance();
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        return calendar.getTimeInMillis();
    }

    //找到未来n天的date，如果n是负数就是找前N天
    public static String getPreNDate(final Date date, int n) {
        if (date == null) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) - n);
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        return df.format(calendar.getTime());
    }

    //找到未来n天的date，如果n是负数就是找前N天
    public static String getPreNDate(final Date date, int n, String format) {
        if (date == null) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) - n);
        SimpleDateFormat df = new SimpleDateFormat(format);
        return df.format(calendar.getTime());
    }

    /**
     * 获得当前月第一天 (yyyy-MM-dd)
     */
    @SuppressWarnings("static-access")
    public static String getFirtDayOfMonth() {
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        Calendar startCal = Calendar.getInstance();
        startCal.set(Calendar.DATE, 1);
        startCal.set(Calendar.HOUR_OF_DAY, 0);
        startCal.set(Calendar.MINUTE, 0);
        startCal.set(Calendar.SECOND, 0);
        startCal.set(Calendar.MILLISECOND, 0);
        return df.format(startCal.getTime());
    }

    /**
     * 获取前几个的日期
     *
     * @param num 1 :前一个月日期 0:当前月日期 -1:后一个月的日期
     * @return
     */
    public static String getDayOfMonth(int num) {
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        Calendar cal = Calendar.getInstance();
        Date date = new Date();
        cal.setTime(date);
        int year = 0;
        int month = cal.get(Calendar.MONTH); // 上个月月份
        cal.set(Calendar.MONTH, month - num);
        if (month == 0) {
            year = cal.get(Calendar.YEAR) - 1;
            month = 12;
        } else {
            year = cal.get(Calendar.YEAR);
        }
        return df.format(cal.getTime());
    }

    /**
     * 获得当前月第一天 (自定义)
     */
    @SuppressWarnings("static-access")
    public static String getFirtDayOfMonth(String format) {
        SimpleDateFormat df = new SimpleDateFormat(format);
        Calendar startCal = Calendar.getInstance();
        startCal.set(startCal.DATE, 1);
        startCal.set(Calendar.HOUR_OF_DAY, 0);
        startCal.set(Calendar.MINUTE, 0);
        startCal.set(Calendar.SECOND, 0);
        startCal.set(Calendar.MILLISECOND, 0);
        return df.format(startCal.getTime());
    }

    /**
     * 获得当前月第一天 (yyyy-MM-dd HH:mm)
     */
    @SuppressWarnings("static-access")
    public static String getFirtDayTimeOfMonth() {
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        Calendar startCal = Calendar.getInstance();
        startCal.set(startCal.DATE, 1);
        startCal.set(Calendar.HOUR_OF_DAY, 0);
        startCal.set(Calendar.MINUTE, 0);
        startCal.set(Calendar.SECOND, 0);
        startCal.set(Calendar.MILLISECOND, 0);
        String day_start = df.format(startCal.getTime());
        return day_start;
    }

    /**
     * 获得当前月第一天 (自定义)
     */
    @SuppressWarnings("static-access")
    public static Date getFirstDayOfMonth(Date date) {
        Calendar cal = Calendar.getInstance();

        cal.setTime(date);

        int last = cal.getActualMinimum(Calendar.DAY_OF_MONTH);

        cal.set(Calendar.DAY_OF_MONTH, last);

        return cal.getTime();
    }

    /**
     * 获得当前月 前/后 第一天
     * num  0:当前月的第一天
     * -1:前一个月的第一天
     * 1:后一个月的第一天
     */
    public static String getFirtDayOfMonthStartTime(int num) {
        Calendar startCal = Calendar.getInstance();
        startCal.add(Calendar.MONTH, num);
        startCal.set(Calendar.DAY_OF_MONTH, 1);
        return convertDateLongToString(startCal.getTimeInMillis(), DateUtil.DEFAULT);
    }

    /**
     * 获得当前月 前/后 第一天
     * num  0:当前月的第一天
     * -1:前一个月的第一天
     * 1:后一个月的第一天
     */
    public static Long getLastDayOfMonthEndTime(int num) {
        Calendar startCal = Calendar.getInstance();
        startCal.add(Calendar.MONTH, num + 1);
        startCal.set(Calendar.DAY_OF_MONTH, 0);
        startCal.set(Calendar.HOUR_OF_DAY, 23);
        startCal.set(Calendar.MINUTE, 59);
        startCal.set(Calendar.SECOND, 59);
        startCal.set(Calendar.MILLISECOND, 999);
        return startCal.getTimeInMillis();
    }

    /**
     * 获取month个月之前的时间 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     */
    public static Long getMonthBeforeDateTime(int month) {
        Calendar startCal = Calendar.getInstance();
        startCal.add(Calendar.MONTH, -month);
        startCal.set(Calendar.HOUR_OF_DAY, 0);
        startCal.set(Calendar.MINUTE, 0);
        startCal.set(Calendar.SECOND, 0);
        startCal.set(Calendar.MILLISECOND, 0);
        Date date = (Date) startCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    /**
     * 获取某个时间几年之前的时间 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     */
    public static Long getYearBeforeDateTime(int year, Long time) {
        Calendar startCal = Calendar.getInstance();
        startCal.setTime(new Date(time));
        startCal.add(Calendar.YEAR, -year);
        Date date = (Date) startCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    /**
     * 获取某个时间几天之前的时间 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     */
    public static Long getDayBeforeDateTime(int day, Long time) {
        Calendar startCal = Calendar.getInstance();
        startCal.setTime(new Date(time));
        startCal.set(Calendar.HOUR_OF_DAY, -day);
        Date date = (Date) startCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    /**
     * 获取某个时间几天之前的时间 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     */
    public static Long getDayBeforeDateTime2(int day, Long time) {
        Calendar startCal = Calendar.getInstance();
        startCal.setTime(new Date(time));
        startCal.set(Calendar.DATE,startCal.get(Calendar.DATE)-day );
        Date date = (Date) startCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    /**
     * 获取某个时间几月之前的时间 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     */
    public static Long getMonthBeforeDateTime(int month, Long time) {
        Calendar startCal = Calendar.getInstance();
        startCal.setTime(new Date(time));
        startCal.add(Calendar.MONTH, -month);
        Date date = (Date) startCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }


    /**
     * 获取当前月第一天 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     */
    @SuppressWarnings("static-access")
    public static Long getFirstDayDateTimeOfMonth() {
        Calendar startCal = Calendar.getInstance();
        startCal.set(startCal.DATE, 1);
        startCal.set(Calendar.HOUR_OF_DAY, 0);
        startCal.set(Calendar.MINUTE, 0);
        startCal.set(Calendar.SECOND, 0);
        startCal.set(Calendar.MILLISECOND, 0);
        Date date = (Date) startCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    /**
     * 获取当前月第一天 (yyyy-MM-dd HH:mm:ss)
     *
     * @return
     * @throws
     */
    @SuppressWarnings("static-access")
    public static Date getFirstDayOfMonth() {
        Calendar startCal = Calendar.getInstance();
        startCal.set(startCal.DATE, 1);
        startCal.set(Calendar.HOUR_OF_DAY, 0);
        startCal.set(Calendar.MINUTE, 0);
        startCal.set(Calendar.SECOND, 0);
        startCal.set(Calendar.MILLISECOND, 0);
        Date date = (Date) startCal.getTime().clone();
        return date;
    }

    /**
     * 获得当前月最后一天 (yyyy-MM-dd)
     */
    @SuppressWarnings("static-access")
    public static String getLastDayOfMonth() {
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        Calendar endCal = Calendar.getInstance();
        endCal.add(Calendar.MONTH, 1);
        endCal.set(Calendar.DATE, 1);
        endCal.add(Calendar.DATE, -1);
        endCal.set(Calendar.HOUR_OF_DAY, 0);
        endCal.set(Calendar.MINUTE, 0);
        endCal.set(Calendar.SECOND, 0);
        endCal.set(Calendar.MILLISECOND, 0);
        String day_end = df.format(endCal.getTime());
        return day_end;
    }

    /**
     * 获得当前月最后一天 (自定义)
     */
    @SuppressWarnings("static-access")
    public static String getLastDayOfMonth(String format) {
        SimpleDateFormat df = new SimpleDateFormat(format);
        Calendar endCal = Calendar.getInstance();
        endCal.add(endCal.MONTH, 1);
        endCal.set(endCal.DATE, 1);
        endCal.add(endCal.DATE, -1);
        endCal.set(Calendar.HOUR_OF_DAY, 0);
        endCal.set(Calendar.MINUTE, 0);
        endCal.set(Calendar.SECOND, 0);
        endCal.set(Calendar.MILLISECOND, 0);
        String day_end = df.format(endCal.getTime());
        return day_end;
    }

    /**
     * 获得当前月上个月第一天
     */
    public static Long geFirstDayOfLastMonth() {
        Calendar cal = Calendar.getInstance();
        Date date = new Date();
        cal.setTime(date);
        int year = 0;
        int month = cal.get(Calendar.MONTH); // 上个月月份
        int day = cal.getActualMinimum(Calendar.DAY_OF_MONTH); // 开始天数
        if (month == 0) {
            year = cal.get(Calendar.YEAR) - 1;
            month = 12;
        } else {
            year = cal.get(Calendar.YEAR);
        }
        return convertDateStringToDateLong("yyyy-MM-dd", (year + "-" + (month) + "-" + day));
    }

    /**
     * 获得当前月上个月最后一天
     */
    public static Long getLastDayOfLastMonth() {
        Calendar cal = Calendar.getInstance();
        Date date = new Date();
        cal.setTime(date);
        int year = 0;
        int month = cal.get(Calendar.MONTH); // 上个月月份
        int day = cal.getActualMaximum(Calendar.DAY_OF_MONTH); // 结束天数
        if (month == 0) {
            year = cal.get(Calendar.YEAR) - 1;
            month = 12;
        } else {
            year = cal.get(Calendar.YEAR);
        }
        return convertDateStringToDateLong("yyyy-MM-dd", (year + "-" + month + "-" + day));
    }

    /**
     * 获得当前时间的月份 有多少天
     */
    public static int getMonthDayCount(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setTimeInMillis(time);
        return cal.getActualMaximum(Calendar.DAY_OF_MONTH);
    }

    /**
     * 获得当前月上个月第一天
     */
    public static Long getFirstDayOfLastMonth() {
        Calendar cal = Calendar.getInstance();
        Date date = new Date();
        cal.setTime(date);
        int year = 0;
        int month = cal.get(Calendar.MONTH); // 上个月月份
        int day = cal.getActualMinimum(Calendar.DAY_OF_MONTH); // 开始天数
        if (month == 0) {
            year = cal.get(Calendar.YEAR) - 1;
            month = 12;
        } else {
            year = cal.get(Calendar.YEAR);
        }
        return convertDateStringToDateLong("yyyy-MM-dd", (year + "-" + month + "-" + day));
    }

    /**
     * 获取当前月最后一天23时59分59秒
     *
     * @return
     * @throws
     */
    @SuppressWarnings("static-access")
    public static Long getLastDayDateTimeOfMonth() {
        Calendar endCal = Calendar.getInstance();
        endCal.add(endCal.MONTH, 1);
        endCal.set(endCal.DATE, 1);
        endCal.add(endCal.DATE, -1);
        endCal.set(Calendar.HOUR_OF_DAY, 23);
        endCal.set(Calendar.MINUTE, 59);
        endCal.set(Calendar.SECOND, 59);
        endCal.set(Calendar.MILLISECOND, 999);
        Date date = (Date) endCal.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    @SuppressWarnings("static-access")
    public static String getLastDayTimeOfMonth() {
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        Calendar cal = Calendar.getInstance();
        cal.add(cal.MONTH, 1);
        cal.set(cal.DATE, 1);
        cal.add(cal.DATE, -1);
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.MILLISECOND, 999);
        String last_day = df.format(cal.getTime());
        return last_day;
    }

    /**
     * 获取指定月最后一天23时59分59秒
     *
     * @return
     * @throws
     */
    @SuppressWarnings("static-access")
    public static Long getLastDayOfMonth(Calendar cal) {
        cal.add(cal.MONTH, 1);
        cal.set(cal.DATE, 1);
        cal.add(cal.DATE, -1);
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.MILLISECOND, 999);
        return cal.getTimeInMillis();
    }

    /**
     * 获取一年的最后一天 23时59分59秒
     *
     * @return
     * @throws
     */
    public static String getEndOfYear() {
        Calendar calendar = Calendar.getInstance();
        calendar.set(Calendar.MONTH, Calendar.DECEMBER);
        calendar.set(Calendar.DAY_OF_MONTH, 31);
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        Date date = (Date) calendar.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return format.format(date);
    }


    public static Long getEndOfYearOfLong() {
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", getEndOfYear());
    }

    /**
     * 获取当年的开始 00时00分00秒
     *
     * @return
     * @throws
     */
    public static String getStartOfYear() {
        Calendar calendar = Calendar.getInstance();
        calendar.set(Calendar.MONTH, Calendar.JANUARY);
        calendar.set(Calendar.DAY_OF_MONTH, 1);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        Date date = (Date) calendar.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return format.format(date);
    }

    /**
     * 获取当年的开始 00时00分00秒
     *
     * @return
     * @throws
     */
    public static Long getStartOfYearOfLong() {
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", getStartOfYear());
    }

    public static Long convertDateDateShortToDateLong(String format, Date date) throws Exception {
        DateFormat df = new SimpleDateFormat(format);
        String dateStr = df.format(date);
        return convertDateStringToDateLong(format, dateStr);
    }

    //将date格式转化为string 格式
    public static String convertDateToDateString(String format, Date date) {
        if (StringUtils.isNotEmpty(format) && date != null) {
            try {
                DateFormat df = new SimpleDateFormat(format);
                return df.format(date);
            } catch (Exception e) {
                LOG.error("convertDateToDateString  EcovacsValidateException error!", e);
            }
        }
        return "";
    }

    public static Long getToday(String format, Date date) {
        DateFormat df = new SimpleDateFormat(format);
        return convertDateStringToDateLong(format, df.format(date));
    }

    public static String getNowTimeStr(String format) {
        return format(format, new Date());
    }

    public static Date getDateFromString(String format, String dateString) {
        if (SysUtil.isEmpty(dateString)) {
            return null;
        }
        SimpleDateFormat simpleDateFormat = new SimpleDateFormat(format);
        Date date;
        try {
            date = simpleDateFormat.parse(dateString);
        } catch (Exception e) {
            date = null;
            LOG.error("getDateFromString EcovacsValidateException error!", e);
        }
        return date;
    }

    /**
     * 获取特定日期开始时间：00：00：00
     *
     * @return 特定日0时0分0秒
     * @throws
     */
    public static Long getStartTimeOfDate(String dateStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(DateUtil.DATE_STRING_FORMAT_DAY, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        Date startDate = (Date) cal.getTime().clone();
        DateFormat format = new SimpleDateFormat(DATE_STRING_FORMAT_ALL);
        return convertDateStringToDateLong(DATE_STRING_FORMAT_ALL, format.format(startDate));
    }

    /**
     * 获取特定日期开始时间：00：00：00:000
     *
     * @return 特定日0时0分0秒0毫秒
     * @throws
     */
    public static Long getStartTimeOfDateStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    /**
     * 获取周一的开始时间
     */
    public static Long getStartTimeOfWeekStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        return getStartTimeOfWeek(dateLong);
    }

    /**
     * 获取月1号的开始时间
     */
    public static Long getStartTimeOfMonthStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        return getStartTimeOfMonth(dateLong);
    }

    public static Long getLastTimeOfMonth15(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }

        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        return getEndMonthHalf(dateLong, 0);
    }

    public static Long getStartTimeOfMonth15(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }

        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        return getBeforeMonthHalf(dateLong, 0);
    }

    public static Long getStartTimeOfDateStr(String dateStr, String formatStr, Locale locale) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr, locale);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    //获取特定时间的开始毫秒 hh：mm：ss:0
    public static Long getStartTimeOfSecondStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    /**
     * 获取特定日期结束时间：23：59：59:999
     *
     * @return 特定日23时59分59秒:999毫秒
     * @throws
     */
    public static Long getEndTimeOfDateStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.MILLISECOND, 999);
        return cal.getTimeInMillis();
    }

    /**
     * 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     *
     * @return 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     * @throws
     */
    public static Long getEndTimeOfMonthStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) + 1);
        cal.set(Calendar.DATE, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis() - 1;
    }

    public static Long getEndTimeOfMonthStr(String dateStr, String formatStr, Locale locale) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr, locale);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) + 1);
        cal.set(Calendar.DATE, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis() - 1;
    }

    //距离本月结束还有多少天
    public static int getEndDaysOfMonth() {
        Calendar cal = Calendar.getInstance();
        int currentDays = cal.get(Calendar.DAY_OF_MONTH);
        cal.set(Calendar.DATE, 1);
        cal.roll(Calendar.DATE, -1);
        int maxDate = cal.get(Calendar.DATE);
        return maxDate - currentDays + 1;
    }

    /**
     * 获取特定日期结束时间：hh：mm：ss:999
     */
    public static Long getEndTimeOfSecondStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.MILLISECOND, 999);
        return cal.getTimeInMillis();
    }

    /**
     * 获取特定日期开始时间：00：00：00
     *
     * @return 特定日0时0分0秒
     * @throws
     */
    public static Date getStartTimeOfDate(Date date) {
        if (date == null) {
            return null;
        }
        Calendar cal = Calendar.getInstance();
        cal.setTime(date);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTime();
    }

    public static Long getStartTimeOfDateLong(Date date) {
        if (date == null) {
            return null;
        }
        Calendar cal = Calendar.getInstance();
        cal.setTime(date);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTime().getTime();
    }

    /**
     * 获取特定日期23：59：59
     *
     * @return 特定日期23：59：59
     * @throws
     */
    public static Date getEndTimeOfDate(Date date) {
        if (date == null) {
            return null;
        }
        Calendar cal = Calendar.getInstance();
        cal.setTime(date);
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.MILLISECOND, 999);
        return cal.getTime();
    }

    public static Long getNextMonthTime(long currentTime) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(currentTime));
        cal.add(Calendar.MONTH, 1);
        return cal.getTime().getTime();
    }

    /**
     * 获取当前天开始时间：00：00：00
     *
     * @return 当前天0时0分0秒
     * @throws
     */
    public static Long getStartTimeOfToday() {
        Calendar currentDate = Calendar.getInstance();
        currentDate.set(Calendar.HOUR_OF_DAY, 0);
        currentDate.set(Calendar.MINUTE, 0);
        currentDate.set(Calendar.SECOND, 0);
        currentDate.set(Calendar.MILLISECOND, 0);
        Date date = (Date) currentDate.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return convertDateStringToDateLong("yyyy-MM-dd HH:mm:ss", format.format(date));
    }

    /**
     * 获取当前天23：59：59
     * 1.1 modify by qxy 当前天23：59：59.999  modifyTime 2013-11-13 15:59
     *
     * @return 当前天23：59：59.999
     * @throws
     */
    public static Long getEndTimeOfToday() {
        Calendar currentDate = Calendar.getInstance();
        currentDate.set(Calendar.HOUR_OF_DAY, 23);
        currentDate.set(Calendar.MINUTE, 59);
        currentDate.set(Calendar.SECOND, 59);
        currentDate.set(Calendar.MILLISECOND, 999);
        return currentDate.getTimeInMillis();
    }

    /**
     * 获取一个月前的那天的起点时间 根据format返回格式
     * 如今天是2017-03-27  format为yyyy-MM-dd HH:mm:ss
     * 返回结果为2017-02-27 00：00：00
     *
     * @return
     */
    public static String getOneMothAgoStartTimeStr(String format) {
        Calendar currentDate = Calendar.getInstance();
        currentDate.add(Calendar.MONTH, -1);
        currentDate.set(Calendar.HOUR_OF_DAY, 0);
        currentDate.set(Calendar.MINUTE, 0);
        currentDate.set(Calendar.SECOND, 0);
        currentDate.set(Calendar.MILLISECOND, 0);
        return convertDateLongToDateString(format, currentDate.getTimeInMillis());
    }

    /**
     * 判断目标时间是否在今天
     *
     * @param targetDate
     * @return
     */
    public static boolean isInToday(Long targetDate) {
        if (targetDate == null) {
            return false;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        Long startOfToday = calendar.getTimeInMillis();
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        Long endOfToday = calendar.getTimeInMillis();
        if (targetDate >= startOfToday && targetDate <= endOfToday) {
            return true;
        }
        return false;
    }

    /**
     * 判断一个data 是否是今天
     *
     * @param targetDate Date
     * @return true 表示是今天,false不是今天,targetDate 为空也返回false
     */
    public static boolean isInToday(Date targetDate) {
        if (targetDate == null) {
            return false;
        }
        return isInToday(targetDate.getTime());
    }

    public static boolean isInYesterday(Long targetDate) {
        if (targetDate == null) {
            return false;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.add(Calendar.DAY_OF_YEAR, -1);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        Long startOfYesterday = calendar.getTimeInMillis();
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        Long endOfYesterday = calendar.getTimeInMillis();
        if (targetDate >= startOfYesterday && targetDate <= endOfYesterday) {
            return true;
        }
        return false;
    }

    /**
     * 判断一个data 是否是昨天
     *
     * @param targetDate Date
     * @return true 表示是昨天,false不是昨天,targetDate 为空也返回false
     */
    public static boolean isInYesterday(Date targetDate) {
        if (targetDate == null) {
            return false;
        }
        return isInYesterday(targetDate.getTime());
    }

    /**
     * 获取昨天：00：00：00:000
     *
     * @return 昨天：00：00：00:000
     * @throws
     */
    public static Long getStartTimeOfYesterday() {
        Calendar currentDate = Calendar.getInstance();
        currentDate.add(Calendar.DAY_OF_YEAR, -1);
        currentDate.set(Calendar.HOUR_OF_DAY, 0);
        currentDate.set(Calendar.MINUTE, 0);
        currentDate.set(Calendar.SECOND, 0);
        currentDate.set(Calendar.MILLISECOND, 0);
        return currentDate.getTimeInMillis();
    }

    /**
     * 获取昨天：00：00：00:000
     *
     * @return 昨天：00：00：00:000
     * @throws
     */
    public static String getStartTimeStrOfYesterday(String format) {
        Long time = getStartTimeOfYesterday();
        return convertDateLongToDateString(format, time);
    }

    /**
     * 获取昨天：00：00：00:000
     *
     * @return 昨天：00：00：00:000
     * @throws
     */
    public static String getStartTimeStrOfToday(String format) {
        Long time = getStartTimeOfToday();
        return convertDateLongToDateString(format, time);
    }

    /**
     * 获取昨天：23：59：59:999
     *
     * @return 昨天：23：59：59: 999
     * @throws
     */
    public static Long getEndTimeOfYesterday() {
        Calendar calendar = Calendar.getInstance();
        calendar.add(Calendar.DAY_OF_YEAR, -1);
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        return calendar.getTimeInMillis();
    }

    /**
     * 获取昨天：23：59：59:999
     *
     * @return 昨天：23：59：59: 999
     * @throws
     */
    public static String getEndTimeStrOfYesterday(String format) {
        Long time = getEndTimeOfYesterday();
        return convertDateLongToDateString(format, time);
    }

    /**
     * 获取昨天：23：59：59:999
     *
     * @return 昨天：23：59：59: 999
     * @throws
     */
    public static String getEndTimeStrOfToday(String format) {
        Long time = getEndTimeOfToday();
        return convertDateLongToDateString(format, time);
    }

    /**
     * 两个日期间的年月日差
     *
     * @param startDate
     * @param endDate
     * @return XX年XX月XX日
     */
    public static String dateDifference(Long startDate, Long endDate, boolean hideDayIfMonth) {
        if (endDate.compareTo(startDate) < 0) {   //保证endDate>startDate
            Long tmp = startDate;
            startDate = endDate;
            endDate = tmp;
        }
        Calendar calS = Calendar.getInstance();
        calS.setTimeInMillis(startDate);
        int startY = calS.get(Calendar.YEAR);
        int startM = calS.get(Calendar.MONTH);
        int startD = calS.get(Calendar.DATE);
        int startDayOfMonth = calS.getActualMaximum(Calendar.DAY_OF_MONTH);
        calS.setTimeInMillis(endDate);
        int endY = calS.get(Calendar.YEAR);
        int endM = calS.get(Calendar.MONTH);
        //处理2011-01-10到2011-01-10，认为服务为一天
        int endD = calS.get(Calendar.DATE) + 1;
        int endDayOfMonth = calS.getActualMaximum(Calendar.DAY_OF_MONTH);
        StringBuilder sBuilder = new StringBuilder();
        int lday = endD - startD;
        if (lday < 0) {
            endM = endM - 1;
            lday = startDayOfMonth + lday;
        }
        //处理天数问题，如：2011-01-01 到 2013-12-31  2年11个月31天     实际上就是3年
        if (lday == endDayOfMonth) {
            endM = endM + 1;
            lday = 0;
        }
        int mos = (endY - startY) * 12 + (endM - startM);
        int lyear = mos / 12;
        int lmonth = mos % 12;
        if (lyear > 0) {
            sBuilder.append(lyear + "年");
        }
        if (lmonth > 0) {
            sBuilder.append(lmonth + "月");
        }
        if (!hideDayIfMonth) {
            if (lday > 0) {
                sBuilder.append(lday + "天");
            }
        } else {
            if (lyear == 0 && lmonth == 0) {
                sBuilder.append(lday + "天");
            }
        }
        return sBuilder.toString();
    }

    public static int fieldDifference(long time1, long time2, int field) {
        if (time1 == time2) {
            return 0;
        } else if (time1 > time2) {
            // 确保time1比time2小
            return fieldDifference(time2, time1, field);
        }
        // 下面清除不要参与比较的内容
        Calendar cal1 = Calendar.getInstance();
        cal1.setLenient(false);
        cal1.setTimeInMillis(time1);
        Calendar cal2 = Calendar.getInstance();
        cal2.setLenient(false);
        cal2.setTimeInMillis(time2);
        for (int x = 0; x < Calendar.FIELD_COUNT; x++) {
            if (x > field) {
                cal1.clear(x);
                cal2.clear(x);
            }
        }
        // 重新设定初始值
        time1 = cal1.getTimeInMillis();
        time2 = cal2.getTimeInMillis();
        long ms = 0;
        int min = 0;// 下限,从0开始
        int max = 1;// 每次所加的值,第一次加1
        while (true) {
            // 因为max的值都是相对time1而言,故每次都是从time1开始而不是ms
            cal1.setTimeInMillis(time1);
            cal1.add(field, max);// 将field对应的字段加上max的值
            ms = cal1.getTimeInMillis();
            if (ms == time2) {
                // 两个时间之间相差的值为max
                min = max;
                break;
            } else if (ms > time2) {
                // 超过后,则差值肯定小于max
                break;
            } else {
                // 仍然小于time2,继续增大max,直到ms>=time2为止
                max <<= 1;
            }
        }
        // 上面的操作中没有找到准确的值,接下来使用二分查找以准确找出差值
        while (max > min) {
            cal1.setTimeInMillis(time1);
            int t = (min + max) >>> 1;
            cal1.add(field, t);
            ms = cal1.getTimeInMillis();
            if (ms == time2) {
                min = t;
                break;
            } else if (ms > time2) {
                max = t;
            } else {
                min = t;
            }
        }
        return min;
    }

    /**
     * 获取过期时间(转换成字符串显示：MM-YY-DD)
     * date为空的时候是根据当前时间算的
     * term为-1L的时候返回-1L(业务中便是无限期)
     *
     * @param term 传入的月份
     * @return
     */
    public static Long getDeadline(Long data, int term) throws Exception {
        if (-1 != term) {
            if (null == data) {
                data = System.currentTimeMillis();
            }
            if (data.longValue() < System.currentTimeMillis()) {
                data = System.currentTimeMillis();
            }
            String dateStr = DateUtil.dateLongToStr(data, DateUtil.YEAR_MONTH_DATE);
            Calendar cal = Calendar.getInstance();
            cal.set(Calendar.YEAR, Integer.parseInt(dateStr.substring(0, 4)));
            cal.set(Calendar.MONTH, Integer.parseInt(dateStr.substring(5, 7)) - 1);
            cal.set(Calendar.DAY_OF_MONTH, Integer.parseInt(dateStr.substring(8, 10)));
            cal.add(Calendar.MONTH, term);
            data = DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE,
                    DateUtil.convertDateLongToDateString(DateUtil.YEAR_MONTH_DATE, cal.getTimeInMillis()));
            return data.longValue() + 86399000L;
        }
        return -1L;
    }

    public static String getTodayStr() {
        return getTodayStr(DateUtil.YEAR_MONTH_DATE);
    }

    public static String getTodayStr(String format) {
        Long date = System.currentTimeMillis();
        return DateUtil.convertDateLongToDateString(format, date);
    }

    public static String getYesterdayStr() {
        return getYesterdayStr(DateUtil.YEAR_MONTH_DATE);
    }

    public static String getYesterdayStr(String format) {
        Calendar c = Calendar.getInstance();
        c.add(Calendar.DATE, -1);
        Long date = c.getTimeInMillis();
        return DateUtil.convertDateLongToDateString(format, date);
    }

    public static int getCurrentYear() {
        Calendar calendar = Calendar.getInstance();
        return calendar.get(Calendar.YEAR);
    }

    public static int getCurrentMonth() {
        Calendar calendar = Calendar.getInstance();
        return calendar.get(Calendar.MONTH) + 1;
    }

    public static int getCurrentDay() {
        Calendar calendar = Calendar.getInstance();
        return calendar.get(Calendar.DAY_OF_MONTH);
    }

    public static int getCurrentHour() {
        Calendar calendar = Calendar.getInstance();
        return calendar.get(Calendar.HOUR_OF_DAY);
    }

    public static int getSecond(Long date) {
        Calendar calendar = Calendar.getInstance();
        ;
        if (null == date) {
            date = System.currentTimeMillis();
        }
        calendar.setTime(new Date(date));
        return calendar.get(Calendar.SECOND);
    }

    public static int getMinute(Long date) {
        Calendar calendar = Calendar.getInstance();
        ;
        if (null == date) {
            date = System.currentTimeMillis();
        }
        calendar.setTime(new Date(date));
        return calendar.get(Calendar.MINUTE);
    }

    public static int getHour(Long date) {
        Calendar calendar = Calendar.getInstance();
        ;
        if (null == date) {
            date = System.currentTimeMillis();
        }
        calendar.setTime(new Date(date));
        return calendar.get(Calendar.HOUR_OF_DAY);
    }

    public static int getYear(Long time) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(time);
        return calendar.get(Calendar.YEAR);
    }

    public static int getMonth(Long time) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(time);
        return calendar.get(Calendar.MONTH) + 1;
    }

    public static int getDay(Long time) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(time);
        return calendar.get(Calendar.DAY_OF_MONTH);
    }

    //判断是否是当前日期
    public static boolean isCurrentTime(Long date) throws Exception {
        if (date == null) {
            return false;
        }
        date = DateUtil.convertDateDateShortToDateLong(DATE_STRING_FORMAT_DAY, new Date(date));
        Long currentDate = DateUtil.convertDateDateShortToDateLong(DATE_STRING_FORMAT_DAY, new Date(System.currentTimeMillis()));
        return (currentDate - date == 0);
    }

    public static Long get6clock(Long date) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(date);
        calendar.set(Calendar.HOUR_OF_DAY, 6);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return calendar.getTimeInMillis();
    }

    /**
     * 标准格式,例如： 2012-04-10
     *
     * @param sDate
     * @return
     */
    public static boolean isStandardDateFormat(String sDate) {
        if (SysUtil.isEmpty(sDate)) {
            return false;
        }
        String importedDatePattern = "^[1-9]\\d{3}\\-(0?[1-9]|1[0-2])\\-(0?[1-9]|1\\d|2\\d|3[0-1])$";
        Pattern pattern = Pattern.compile(importedDatePattern);
        Matcher match = pattern.matcher(sDate);
        return match.matches();
    }

    public static Long getLastWeekTime() {
        Calendar calendar = Calendar.getInstance();
        calendar.add(Calendar.DATE, -7);
        return calendar.getTimeInMillis();
    }

    public static Long getLastWeekTime(Calendar calendar) {
        calendar.add(Calendar.DATE, -7);
        return calendar.getTimeInMillis();
    }

    public static Long getLastMonthTime(Calendar calendar) {
        calendar.add(Calendar.MONTH, -1);
        return calendar.getTimeInMillis();
    }

    public static Long getLastThreeMonthTime(Calendar calendar) {
        calendar.add(Calendar.MONTH, -3);
        return calendar.getTimeInMillis();
    }

    public static Long getLastHalfYearTime(Calendar calendar) {
        calendar.add(Calendar.MONTH, -6);
        return calendar.getTimeInMillis();
    }

    public static Long getLastYearTime(Calendar calendar) {
        calendar.add(Calendar.YEAR, -1);
        return calendar.getTimeInMillis();
    }

    /**
     * 获取指定日期开始时间：00：00：00
     *
     * @return 当前天0时0分0秒
     * @throws
     */
    public static Long getStartOfDate(Long vDate, String formatStr) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(vDate));
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        Date date = (Date) calendar.getTime().clone();
        DateFormat format = new SimpleDateFormat(formatStr);
        return convertDateStringToDateLong(formatStr, format.format(date));
    }

    /**
     * 获取指定日期结束时间23：59：59
     *
     * @return 当前天23：59：59
     * @throws
     */
    public static Long getEndOfDate(Long vDate, String formatStr) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(vDate));
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        Date date = (Date) calendar.getTime().clone();
        DateFormat format = new SimpleDateFormat(formatStr);
        return convertDateStringToDateLong(formatStr, format.format(date));
    }

    public static Long getEndOfDate(Long date) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(date));
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        return calendar.getTime().getTime();
    }

    public static int getWeekOfYear(Long date) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(date);
        calendar.setFirstDayOfWeek(Calendar.MONDAY);
        return calendar.get(Calendar.WEEK_OF_YEAR);
    }

    /**
     * 获取当前月的总天数
     *
     * @return
     */
    public static int getDayOfMonth() {
        Calendar aCalendar = Calendar.getInstance(Locale.CHINA);
        int day = aCalendar.getActualMaximum(Calendar.DATE);
        return day;
    }

    public static int getDaysOfMonth(Date date) {
        Calendar calendar = Calendar.getInstance(Locale.CHINA);
        calendar.setTime(date);
        return calendar.getActualMaximum(Calendar.DATE);
    }

    public static List<String> getDays(String startDate, String endDate) {
        return getDates(startDate, endDate, null, null);
    }

    public static List<String> getDates(String startDate, String endDate, String inFormat, String outFormat) {
        List days = new ArrayList();// 日期集合
        if (SysUtil.isEmpty(inFormat)) {
            inFormat = YEAR_MONTH_DATE;
        }
        if (SysUtil.isEmpty(outFormat)) {
            outFormat = YEAR_MONTH_DATE;
        }
        long end = convertDateStringToDateLong(inFormat, endDate);
        long begin = convertDateStringToDateLong(inFormat, startDate);
        while (begin <= end) {
            Date day = new Date();
            day.setTime(begin);
            begin += 3600 * 24 * 1000;
            days.add(format(outFormat, day));
        }
        return days;
    }

    public static String getHourTime(int dayOffset, int hour) {
        Calendar cal = Calendar.getInstance();
        if (dayOffset != 0)
            cal.add(Calendar.DATE, dayOffset);
        cal.set(Calendar.HOUR_OF_DAY, hour);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        return new SimpleDateFormat("yyyy-MM-dd HH:mm:ss").format(cal.getTime());
    }

    /**
     * 获得当前月最后N天 (date)
     */
    @SuppressWarnings("static-access")
    public static Date getLastDaysOfMonth(int n) {
        Calendar endCal = Calendar.getInstance();
        endCal.add(Calendar.MONTH, 1);
        endCal.set(Calendar.DATE, 1);
        endCal.add(Calendar.DATE, -n);
        endCal.set(Calendar.HOUR_OF_DAY, 0);
        endCal.set(Calendar.MINUTE, 0);
        endCal.set(Calendar.SECOND, 0);
        endCal.set(Calendar.MILLISECOND, 0);
        return endCal.getTime();
    }

    //获取几天中最小的一天
    public static Date getMinDate(final Date... dates) {
        if (ArrayUtils.isEmpty(dates)) {
            return null;
        }
        Date minDate = null;
        for (Date date : dates) {
            if (date != null) {
                if (minDate == null) {
                    minDate = date;
                } else {
                    if (date.getTime() < minDate.getTime()) {
                        minDate = date;
                    }
                }
            }
        }
        return minDate;
    }

    //获取几天中最大的一天
    public static Date getMaxDate(final Date... dates) {
        if (ArrayUtils.isEmpty(dates)) {
            return null;
        }
        Date maxDate = null;
        for (Date date : dates) {
            if (date != null) {
                if (maxDate == null) {
                    maxDate = date;
                } else {
                    if (date.getTime() > maxDate.getTime()) {
                        maxDate = date;
                    }
                }
            }
        }
        return maxDate;
    }

    //获取startDate 和 endDate 两个的日期差
    public static int getSubDayFromDate(Date startDate, Date endDate) {
        if (startDate != null && endDate != null) {
            Calendar startCalendar = Calendar.getInstance();
            startCalendar.setTime(startDate);
            startCalendar.set(Calendar.HOUR_OF_DAY, 0);
            startCalendar.set(Calendar.MINUTE, 0);
            startCalendar.set(Calendar.SECOND, 0);
            startCalendar.set(Calendar.MILLISECOND, 0);
            Calendar endCalendar = Calendar.getInstance();
            endCalendar.setTime(endDate);
            endCalendar.set(Calendar.HOUR_OF_DAY, 0);
            endCalendar.set(Calendar.MINUTE, 0);
            endCalendar.set(Calendar.SECOND, 0);
            endCalendar.set(Calendar.MILLISECOND, 0);
            return (int) ((endCalendar.getTimeInMillis() - startCalendar.getTimeInMillis()) / 1000 / 60 / 60 / 24);
        }
        return 0;
    }

    public static int daysBetween(String start, String end) throws Exception {
        SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
        Calendar cal = Calendar.getInstance();
        cal.setTime(sdf.parse(start));
        long time1 = cal.getTimeInMillis();
        cal.setTime(sdf.parse(end));
        long time2 = cal.getTimeInMillis();
        long between_days = (time2 - time1) / (1000 * 3600 * 24);
        //求绝对值
        return Math.abs(Integer.parseInt(String.valueOf(between_days)));
    }

    //获取一个距离当前时间的天数 绝对值
    public static int daysBetweenNow(Date date) {
        long betweenDays = (System.currentTimeMillis() - date.getTime()) / (1000 * 3600 * 24);
        //求绝对值
        return Math.abs(Integer.parseInt(String.valueOf(betweenDays)));
    }

    public static String getCurrentTimestampStr(String format) {
        return format(format, new Date());
    }

    /**
     * 显示在途耗时，精确到分钟，例如 3天21小时30分
     *
     * @param time
     * @return
     */
    public static String timeLong2CountTimeString(Long time) {
        StringBuilder stringBuilder = new StringBuilder();
        Long day = time / (24 * 3600 * 1000);
        if (day > 0) {
            stringBuilder.append(day + "天");
        }
        Long hour = time % (24 * 3600 * 1000) / (3600 * 1000);
        if (hour > 0) {
            stringBuilder.append(hour + "小时");
        }
        Long minute = time % (24 * 3600 * 1000) % (3600 * 1000) / (60 * 1000);
        if (minute > 0) {
            stringBuilder.append(minute + "分");
        }
        return stringBuilder.toString();
    }

    /**
     * 获取几天后天的时间
     *
     * @param n
     * @param hour
     * @param minute
     * @return
     */
    public static Long getInnerDayTime(int n, int hour, int minute) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(System.currentTimeMillis());
        calendar.set(Calendar.HOUR_OF_DAY, hour);
        calendar.set(Calendar.MINUTE, minute);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        calendar.set(Calendar.DATE, calendar.get(Calendar.DATE) + n);
        return calendar.getTimeInMillis();
    }

    //获取一天的开始时间
    public static Long getStartTimeOfDay(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setFirstDayOfWeek(Calendar.MONDAY);
        cal.setTime(new Date(time));
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    //获取一天的结束时间
    public static Long getEndTimeOfDay(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setFirstDayOfWeek(Calendar.MONDAY);
        cal.setTime(new Date(time));
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.MILLISECOND, 999);
        return cal.getTimeInMillis();
    }


    //获取time所在周的周一的开始日期
    public static Long getStartTimeOfWeek(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        if (cal.get(Calendar.DAY_OF_WEEK) == Calendar.SUNDAY) {
            cal.add(Calendar.DATE, Calendar.MONDAY - cal.get(Calendar.DAY_OF_WEEK) - 7);
        } else {
            cal.add(Calendar.DATE, Calendar.MONDAY - cal.get(Calendar.DAY_OF_WEEK));
        }
        return cal.getTimeInMillis();
    }

    //获取上周，周日的开始时间
    public static Long getEndTimeOfLastWeek() {
        return getStartTimeOfWeek(System.currentTimeMillis()) - DateUtil.DAY_MILLION_SECONDS;
    }

    //获取每月1号的开始时间
    public static Long getStartTimeOfMonth(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.DAY_OF_MONTH, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    public static String getStartTimeStrOfMonth(String format) {
        Calendar cal = Calendar.getInstance();
        cal.setTimeInMillis(System.currentTimeMillis());
        cal.set(Calendar.DAY_OF_MONTH, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return format(format, cal.getTime());
    }

    /**
     * 获取n个月之前1/15号的开始时间
     *
     * @param time
     * @param n    1:前一个月    0:当前月   -1:后一个月
     * @return
     */
    public static Long getStartTimeOfBeforeHalfMonth(Long time, Integer n) {
        //默认为当前月
        n = NumberUtil.intValue(n);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) - n);
        int day = cal.get(Calendar.DAY_OF_MONTH);    //获取当前天数
//
        if (day <= 15) {
            cal.set(Calendar.DAY_OF_MONTH, 1);
        } else {
            cal.set(Calendar.DAY_OF_MONTH, 16);
        }
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }


    /**
     * 获取n个月之前 的15号
     *
     * @param time
     * @param n    1:前一个月    0:当前月   -1:后一个月
     * @return
     */
    public static Long getBeforeMonthHalf(Long time, Integer n) {
        //默认为当前月
        n = NumberUtil.intValue(n);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) - n);
        cal.set(Calendar.DAY_OF_MONTH, 15);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    /**
     * 获取n个月之前 的15号 结束
     *
     * @param time
     * @param n    1:前一个月    0:当前月   -1:后一个月
     * @return
     */
    public static Long getEndMonthHalf(Long time, Integer n) {
        //默认为当前月
        n = NumberUtil.intValue(n);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) - n);
        cal.set(Calendar.DAY_OF_MONTH, 15);
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    /**
     * 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     *
     * @return 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     * @throws
     */
    public static Long getEndTimeOfHalfMonth(Long time) {
        Long value = null;
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        int day = cal.get(Calendar.DAY_OF_MONTH);    //获取当前天数
//
        if (day <= 15) {
            cal.set(Calendar.MONTH, cal.get(Calendar.MONTH));
            cal.set(Calendar.DAY_OF_MONTH, 15);
            cal.set(Calendar.HOUR_OF_DAY, 23);
            cal.set(Calendar.MINUTE, 59);
            cal.set(Calendar.SECOND, 59);
            cal.set(Calendar.MILLISECOND, 0);
            value = cal.getTimeInMillis();
        } else {
            cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) + 1);
            cal.set(Calendar.DAY_OF_MONTH, 1);
            cal.set(Calendar.DATE, 1);
            cal.set(Calendar.HOUR_OF_DAY, 0);
            cal.set(Calendar.MINUTE, 0);
            cal.set(Calendar.SECOND, 0);
            cal.set(Calendar.MILLISECOND, 0);
            value = cal.getTimeInMillis() - 1;
        }

        return value;
    }

    /**
     * 获取n个月之前1号的开始时间
     *
     * @param time
     * @param n    1:前一个月    0:当前月   -1:后一个月
     * @return
     */
    public static Long getStartTimeOfBeforeMonth(Long time, Integer n) {
        //默认为当前月
        n = NumberUtil.intValue(n);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) - n);
        cal.set(Calendar.DAY_OF_MONTH, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }


    /**
     * 获取n个年当天的开始时间
     *
     * @param time
     * @param n    1:前年    0:当年   -1:后年
     * @return
     */
    public static Long getDayStartTimeOfBeforeYear(Long time, Integer n) {
        //默认为当前年
        n = NumberUtil.intValue(n);
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(time));
        calendar.set(Calendar.YEAR, calendar.get(Calendar.YEAR) - n);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return calendar.getTimeInMillis();
    }

    /**
     * 获取n个年当天的开始时间
     *
     * @param time
     * @param n    1:前年    0:当年   -1:后年
     * @return
     */
    public static Long getDayEndTimeOfBeforeYear(Long time, Integer n) {
        //默认为当前年
        n = NumberUtil.intValue(n);
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(time));
        calendar.set(Calendar.YEAR, calendar.get(Calendar.YEAR) - n);
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        return calendar.getTimeInMillis();
    }


    /**
     * 获取n个年当天的开始时间
     *
     * @param time
     * @param n    1:前年    0:当年   -1:后年
     * @return
     */
    public static Long getEndTimeOfBeforeYear(Long time, Integer n) {
        //默认为当前年
        n = NumberUtil.intValue(n);
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(time));
        calendar.set(Calendar.YEAR, calendar.get(Calendar.YEAR) - n);
        return calendar.getTimeInMillis();
    }

    /**
     * 获取n个年之前1月1号的开始时间
     *
     * @param time
     * @param n    1:前年    0:当年   -1:后年
     * @return
     */
    public static Long getStartTimeOfBeforeYear(Long time, Integer n) {
        //默认为当前年
        n = NumberUtil.intValue(n);
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(time));
        calendar.set(Calendar.YEAR, calendar.get(Calendar.YEAR) - n);
        calendar.set(Calendar.MONTH, Calendar.JANUARY);
        calendar.set(Calendar.DAY_OF_MONTH, 1);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return calendar.getTimeInMillis();
    }

    /**
     * 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     *
     * @return 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     * @throws
     */
    public static Long getEndTimeOfMonth(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) + 1);
        cal.set(Calendar.DATE, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis() - 1;
    }

    /**
     * 获取特定当前的的结束时间：该月的最后一天的23：59：59:999
     *
     * @return 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     * @throws
     */
    public static Long getStartTimeOfToday(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) - 1);
        cal.set(Calendar.DATE, Calendar.DAY_OF_MONTH - 2);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    /**
     * 获取特定当前的的结束时间：该月的最后一天的23：59：59:999
     *
     * @return 获取特定月份的结束时间：该月的最后一天的23：59：59:999
     * @throws
     */
    public static Long getEndTimeOfToday(Long time) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        return cal.getTimeInMillis();
    }

    /**
     * 获取特定月份的结束时间：该年的最后一天的23：59：59:999
     *
     * @return 获取特定月份的结束时间：该年的最后一天的23：59：59:999
     * @throws
     */
    public static Long getEndTimeOfYear(Long time) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(new Date(time));
        calendar.set(Calendar.MONTH, Calendar.DECEMBER);
        calendar.set(Calendar.DAY_OF_MONTH, 31);
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        return calendar.getTimeInMillis();
    }

    //获取上月最后一天的开始时间
    public static Long getEndTimeOfLastMonth() {
        Calendar cal = Calendar.getInstance();
        cal.set(Calendar.DAY_OF_MONTH, 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        cal.add(Calendar.DATE, -1);
        return cal.getTimeInMillis();
    }

    //获取两个日期之间差了几个月
    //例如：2016-07-09 到 2017-08-02 相差13个月
    public static int getMonthBetween(Long startTime, Long endTime) {
        Calendar startCal = Calendar.getInstance();
        startCal.setTime(new Date(startTime));
        Calendar endCal = Calendar.getInstance();
        endCal.setTime(new Date(endTime));
        return (endCal.get(Calendar.YEAR) - startCal.get(Calendar.YEAR)) * 12
                + endCal.get(Calendar.MONTH) - startCal.get(Calendar.MONTH);
    }

    //当前时间，变化 n 个月后的时间
    public static Long getNextMonthTime(long startTime, int n) {
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(startTime));
        cal.add(Calendar.MONTH, n);
        return cal.getTime().getTime();
    }

    /**
     * 获取几个星期之后的星期几
     *
     * @param week    星期数
     * @param weekDay 星期几（0=星期日，7=星期六）
     * @param hour
     * @param minute
     * @return
     */
    public static Long getNextWeekDay(int week, int weekDay, int hour, int minute, Long beginTime) {
        int addDay = week * 7 + weekDay;
        Calendar cal = Calendar.getInstance();
        if (beginTime != null && beginTime > 0) {
            cal.setTimeInMillis(beginTime);
        }
        cal.set(Calendar.DAY_OF_WEEK, 1);
        cal.set(Calendar.HOUR_OF_DAY, hour);
        cal.set(Calendar.MINUTE, minute);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        cal.add(Calendar.DATE, addDay);
        return cal.getTimeInMillis();
    }

    /**
     * 获取几个月之后的正数/倒数第几天
     *
     * @param month  月数
     * @param day    天数
     * @param hour
     * @param minute
     * @param type   1=正数、2=倒数
     * @return
     */
    public static Long getNextMonthDayByType(int month, int day, int hour, int minute, int type, Long beginTime) {
        Calendar cal = Calendar.getInstance();
        if (beginTime != null && beginTime > 0) {
            cal.setTimeInMillis(beginTime);
        }
        cal.set(Calendar.DAY_OF_MONTH, 1);
        cal.set(Calendar.HOUR_OF_DAY, hour);
        cal.set(Calendar.MINUTE, minute);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        cal.add(Calendar.MONTH, month);
        if (type == 1) { // 正数
            cal.add(Calendar.DAY_OF_MONTH, day - 1);
        } else if (type == 2) { // 倒数
            cal.add(Calendar.MONTH, 1);
            cal.add(Calendar.DATE, -day);
        }
        return cal.getTimeInMillis();
    }

    /**
     * 获取X年后的今天
     *
     * @param year
     * @return
     */
    public static Long getNextYear(int year) {
        Calendar cal = Calendar.getInstance();
        cal.add(Calendar.YEAR, year);
        return cal.getTimeInMillis();
    }

    /**
     * 获取X天Y小时Z分钟之后的时间
     *
     * @param day
     * @return minute
     */
    public static Long getNextDayHourMinute(int day, int hour, int minute) {
        Calendar cal = Calendar.getInstance();
        cal.add(Calendar.DATE, day);
        cal.add(Calendar.HOUR, hour);
        cal.add(Calendar.MINUTE, minute);
        return cal.getTimeInMillis();
    }

    /**
     * 获取day之后的零点
     *
     * @param day 可为负数
     * @return
     */
    public static Long getNextDayBeginTime(int day) {
        Calendar cal = Calendar.getInstance();
        cal.add(Calendar.DATE, day);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    /**
     * 获取该时间后一天的23：59：59:999
     *
     * @return 获取该时间后一天的23：59：59:999
     * @throws
     */
    public static Long getAfterEndTimeOfDateStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.HOUR_OF_DAY, 23);
        cal.set(Calendar.MINUTE, 59);
        cal.set(Calendar.SECOND, 59);
        cal.set(Calendar.DATE, cal.get(Calendar.DATE) + 1);
        cal.set(Calendar.MILLISECOND, 999);
        return cal.getTimeInMillis();
    }

    /**
     * 拿下一个时分秒
     *
     * @param hour
     * @param minute
     * @return
     */
    public static Long getNextHourMinute(int hour, int minute) {
        Calendar cal = Calendar.getInstance();
        cal.set(Calendar.HOUR_OF_DAY, hour);
        cal.set(Calendar.MINUTE, minute);
        if (getCurrentHour() > hour) {
            cal.set(Calendar.DATE, cal.get(Calendar.DATE) + 1);
        }
        return cal.getTimeInMillis();
    }

    /**
     * 获取该时间后一天开始时间的23：59：59:999
     *
     * @return 获取该时间后一天开始时间的23：59：59:999
     * @throws
     */
    public static Long getAfterStartTimeOfDateStr(String dateStr, String formatStr) {
        if (SysUtil.isEmpty(dateStr)) {
            return null;
        }
        Long dateLong = convertDateStringToDateLong(formatStr, dateStr);
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(dateLong));
        cal.set(Calendar.DATE, cal.get(Calendar.DATE) + 1);
        cal.set(Calendar.HOUR_OF_DAY, 0);
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        cal.set(Calendar.MILLISECOND, 0);
        return cal.getTimeInMillis();
    }

    //获取date的星期,返回值是 1-7,星期天:1 ,星期一:2,星期二:3,星期三:4,星期四:5,星期五:6,星期六:7
    public static WeekDayEnum getWeekDayEnumFromDate(Date date) {
        if (date == null) {
            return null;
        }
        Calendar cal = Calendar.getInstance();
        cal.setTime(date);
        //返回值是 1-7,星期天:1 ,星期一:2,星期二:3,星期三:4,星期四:5,星期五:6,星期六:7
        int dayOfWeek = cal.get(Calendar.DAY_OF_WEEK);
        switch (dayOfWeek) {
            case Calendar.SUNDAY:
                return WeekDayEnum.SUN;
            case Calendar.MONDAY:
                return WeekDayEnum.MON;
            case Calendar.TUESDAY:
                return WeekDayEnum.TUE;
            case Calendar.WEDNESDAY:
                return WeekDayEnum.WED;
            case Calendar.THURSDAY:
                return WeekDayEnum.THU;
            case Calendar.FRIDAY:
                return WeekDayEnum.FRI;
            case Calendar.SATURDAY:
                return WeekDayEnum.SAT;
            default:
                return null;
        }
    }

    /**
     * 根据时间戳，判断星期几
     *
     * @param millSec(毫秒数)
     * @return
     */
    public static WeekDayEnum getWeekDayByTime(Long millSec) {
        if (null == millSec) {
            return null;
        }
        SimpleDateFormat sdf = new SimpleDateFormat(DateUtil.ALL);
        Date date = new Date(millSec);
        return getWeekDayEnumFromDate(date);
    }

    /**
     * @param currentTime 当前时间
     * @param minute      之前/之后的分钟数    -5(前5分钟)  5(后五分钟)
     * @param formatStr   当前时间
     * @return
     */
    public static String getTimeByMinute(Long currentTime, Integer minute, String formatStr) {
        if (SysUtil.isEmpty(formatStr) || null == currentTime || null == minute) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTimeInMillis(currentTime);
        calendar.add(Calendar.MINUTE, minute);
        return new SimpleDateFormat(formatStr).format(calendar.getTime());
    }

    /**
     * 计算出2个时间,相差的天数
     *
     * @param date1
     * @param date2
     * @return
     * @throws ParseException
     */
    public static long getDateSpace(String date1, String date2) {
        SimpleDateFormat sdf = new SimpleDateFormat(DateUtil.DEFAULT);
        try {
            Date d1 = sdf.parse(date1);
            Date d2 = sdf.parse(date2);
            long daysBetween = (d2.getTime() - d1.getTime()) / (3600 * 24 * 1000);
            return daysBetween;
        } catch (Exception e) {
            throw new EcovacsValidateException("日期格式有误!");
        }
    }

    public static String getBeforeDate(Long dateLong) {
        Calendar c = Calendar.getInstance();
        if (null == dateLong) {
            dateLong = System.currentTimeMillis();
        }
        Date date = new Date(dateLong);
        c.setTime(date);
        int day = c.get(Calendar.DATE);
        c.set(Calendar.DATE, day - 1);
        return convertDateToDateString(DateUtil.YEAR_MONTH_DATE, c.getTime());
    }

    public static String getAfterDate(Long dateLong, String type, int add) {
        Calendar c = Calendar.getInstance();
        if (null == dateLong) {
            dateLong = System.currentTimeMillis();
        }
        Date date = new Date(dateLong);
        c.setTime(date);
        if ("day".equals(type)) {
            int day = c.get(Calendar.DATE);
            c.set(Calendar.DATE, day + add);
        } else if ("week".equals(type)) {
            int day = c.get(Calendar.DATE);
            c.set(Calendar.DATE, day + (add * 7));
        } else if ("mouth".equals(type)) {
            int mouth = c.get(Calendar.MONTH);
            c.set(Calendar.MONTH, mouth + add);
        }

        return convertDateToDateString(DateUtil.YEAR_MONTH_DATE, c.getTime());
    }

    public static Long getAfterDateLong(Long dateLong, String type, int add) {
        Calendar c = Calendar.getInstance();
        if (null == dateLong) {
            dateLong = System.currentTimeMillis();
        }
        Date date = new Date(dateLong);
        c.setTime(date);
        if ("day".equals(type)) {
            int day = c.get(Calendar.DATE);
            c.set(Calendar.DATE, day + add);
        } else if ("week".equals(type)) {
            int day = c.get(Calendar.DATE);
            c.set(Calendar.DATE, day + (add * 7));
        } else if ("mouth".equals(type)) {
            int mouth = c.get(Calendar.MONTH);
            c.set(Calendar.MONTH, mouth + add);
        }

        return c.getTimeInMillis();
    }

    /**
     * 校验日期格式是否合法
     *
     * @param time   日期str
     * @param format 期望的日期格式
     * @return
     */
    public static Boolean validateDateFormat(String time, String format) {
        if (time == null || time.trim().equals("")) {
            return false;
        }
        SimpleDateFormat fmt = new SimpleDateFormat(format);
        Date d = null;
        try {
            d = fmt.parse(time);
            return true;
        } catch (Exception e) {
            LOG.warn("validateDateFormat warn");
        }
        return false;
    }

    /**
     * 获取上一次的凌晨3点钟的毫秒数
     * 如果有指定结束日期,返回指定时间的上一个凌晨3点的毫秒数
     * 如果无指定时间，返回当前时间的上一个凌晨3点的毫秒数
     *
     * @return
     */
    public static Long getLastThreeOClockMTime(Long millisTime) {
        if (millisTime == null) {
            millisTime = System.currentTimeMillis();
        }
        int hour = DateUtil.getHour(millisTime);
        if (0 <= hour && hour < 3) {
            //获取前一天03：00的时间
            return DateUtil.convertDateStringToDateLong(DateUtil.DATE_STRING_FORMAT_ALL, DateUtil.getBeforeDate(millisTime) + " 03:00:00");
        }
        //获取指定日期03:00的时间
        String dateStr = DateUtil.convertDateToDateString(DateUtil.YEAR_MONTH_DATE, new Date(millisTime));
        return DateUtil.convertDateStringToDateLong(DateUtil.DATE_STRING_FORMAT_ALL, dateStr + " 03:00:00");
    }

    /**
     * 返回当前时间的最大毫秒数（默认当前时间毫秒数都是0）。
     *
     * @param time 当前时间
     * @return
     */
    public static Long convertDateStringToDateLongWithMaxMillisecond(String time) {
        return convertDateStringToDateLong(ALL_INCLUDE_MILLISECOND, time + ":999");
    }

    /**
     * 获取两个日期之间的所有日期（yyyy-MM-dd）
     */
    public static List<String> getBetweenDates(String beginStr, String endStr) {
        SimpleDateFormat sdf = new SimpleDateFormat(DateUtil.DEFAULT);
        List<String> result = new ArrayList<>();
        try {
            Date begin = sdf.parse(beginStr);
            Date end = sdf.parse(endStr);
            Calendar tempStart = Calendar.getInstance();
            tempStart.setTime(begin);
            while (begin.getTime() <= end.getTime()) {
                result.add(sdf.format(tempStart.getTime()));
                tempStart.add(Calendar.DAY_OF_YEAR, 1);
                begin = tempStart.getTime();
            }
        } catch (Exception e) {
            LOG.error("getBetweenDates  EcovacsValidateException error!", e);
        }
        return result;
    }

    /**
     * 获取两个日期之间的所有日期
     */
    public static List<String> getBetweenDates(Long beginTime, Long endTime, String format) {
        SimpleDateFormat sdf = new SimpleDateFormat(format);
        List<String> result = new ArrayList<>();
        try {
            Date begin = new Date();
            begin.setTime(beginTime);
            Date end = new Date();
            end.setTime(endTime);
            Calendar tempStart = Calendar.getInstance();
            tempStart.setTime(begin);
            while (begin.getTime() <= end.getTime()) {
                result.add(sdf.format(tempStart.getTime()));
                tempStart.add(Calendar.DAY_OF_YEAR, 1);
                begin = tempStart.getTime();
            }
        } catch (Exception e) {
            LOG.error("getBetweenDates  EcovacsValidateException error!", e);
        }
        return result;
    }

    /**
     * 获取两个日期之间的所有月份
     */
    public static List<String> getMonthBetweenDates(Long beginTime, Long endTime, String format) {
        SimpleDateFormat sdf = new SimpleDateFormat(format);
        List<String> result = new ArrayList<>();
        try {
            Date begin = new Date();
            begin.setTime(beginTime);
            Date end = new Date();
            end.setTime(endTime);
            Calendar tempStart = Calendar.getInstance();
            tempStart.setTime(begin);
            while (begin.getTime() <= end.getTime()) {
                result.add(sdf.format(tempStart.getTime()));
                tempStart.add(Calendar.MONTH, 1);
                begin = tempStart.getTime();
            }
        } catch (Exception e) {
            LOG.error("getBetweenDates  EcovacsValidateException error!", e);
        }
        return result;
    }

    /**
     * 获取最近月份
     * @param n 几个月
     * @return
     */
    public static List<String> monthBetween(int n) {
        SimpleDateFormat sdf = new SimpleDateFormat("yy-MM");
        List<String> result = new ArrayList<>();
        try {
            Calendar endCal = Calendar.getInstance();
            while (result.size() < n) {
                result.add(sdf.format(endCal.getTime()));
                endCal.add(Calendar.MONTH, -1);
            }
        } catch (Exception e) {
            LOG.error("getBetweenDates  EcovacsValidateException error!", e);
        }
        return result;
    }

    /**
     * 获取一天之中所有小时的分组
     */
    public static List<String> getHourInDay() {
        List<String> result = new ArrayList<>();
        result.add("00");
        result.add("01");
        result.add("02");
        result.add("03");
        result.add("04");
        result.add("05");
        result.add("06");
        result.add("07");
        result.add("08");
        result.add("09");
        result.add("10");
        result.add("11");
        result.add("12");
        result.add("13");
        result.add("14");
        result.add("15");
        result.add("16");
        result.add("17");
        result.add("18");
        result.add("19");
        result.add("20");
        result.add("21");
        result.add("22");
        result.add("23");
        return result;
    }

    /**
     * 获取今天的最后时间 23时59分59秒
     *
     * @return
     * @throws
     */
    public static String getEndOfToday() {
        Calendar calendar = Calendar.getInstance();
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);
        Date date = (Date) calendar.getTime().clone();
        DateFormat format = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
        return format.format(date);
    }

    /**
     * 获取当前时间 num 月后的日期
     *
     * @param time
     * @param format
     * @param num    -1 前一个月   1:后一个月
     * @return
     */
    public static String getYearMonthByTime(String time, String format, int num) {
        Calendar c = Calendar.getInstance();//获得一个日历的实例
        SimpleDateFormat sdf = new SimpleDateFormat(format);
        Date date = null;
        try {
            date = sdf.parse(time);//初始日期
        } catch (Exception e) {
            LOG.error("getYearMonthByTime  EcovacsValidateException error!", e);
        }
        c.setTime(date);//设置日历时间
        c.add(Calendar.MONTH, num);//在日历的月份上增加num月份
        return sdf.format(c.getTime());
    }

    public static int differentDays(String datestr1, String datestr2) {
        Date date1 = convertDateStringToDate(DateUtil.ALL, datestr1);
        Date date2 = convertDateStringToDate(DateUtil.ALL, datestr2);
        Calendar cal1 = Calendar.getInstance();
        cal1.setTime(date1);
        Calendar cal2 = Calendar.getInstance();
        cal2.setTime(date2);
        int day1 = cal1.get(Calendar.DAY_OF_YEAR);
        int day2 = cal2.get(Calendar.DAY_OF_YEAR);
        int year1 = cal1.get(Calendar.YEAR);
        int year2 = cal2.get(Calendar.YEAR);
        if (year1 != year2) //同一年
        {
            int timeDistance = 0;
            for (int i = year1; i < year2; i++) {
                if (i % 4 == 0 && i % 100 != 0 || i % 400 == 0) //闰年
                {
                    timeDistance += 366;
                } else //不是闰年
                {
                    timeDistance += 365;
                }
            }
            return (timeDistance + (day2 - day1) + 1);
        } else //不同年
        {
            return ((day2 - day1) + 1);
        }
    }

    public static GregorianCalendar getDateCalendar(String timeStr) {
        Date dateTime = DateUtil.convertDateStringToDate(DateUtil.ALL, timeStr);
        GregorianCalendar gc = new GregorianCalendar();
        gc.setTimeZone(TimeZone.getTimeZone("GMT"));
        gc.setTime(dateTime);
        return gc;
    }

    /***
     * convert Date to cron ,eg.  "0 07 10 15 1 ? 2016"
     * @param time  : 时间戳
     * @return
     */
    public static String getCron(Long time) {
        return convertDateLongToString(time, DATE_2_CRON);
    }

    /**
     * 当月天数
     *
     * @param year
     * @param month
     * @return
     */
    public static int getMonthLastDay(int year, int month) {
        Calendar a = Calendar.getInstance();
        a.set(Calendar.YEAR, year);
        a.set(Calendar.MONTH, month - 1);
        a.set(Calendar.DATE, 1);//把日期设置为当月第一天
        a.roll(Calendar.DATE, -1);//日期回滚一天，也就是最后一天
        int maxDate = a.get(Calendar.DATE);
        return maxDate;
    }

    /**
     * 取得当月天数
     */
    public static int getCurrentMonthLastDay() {
        Calendar a = Calendar.getInstance();
        a.set(Calendar.DATE, 1);//把日期设置为当月第一天
        a.roll(Calendar.DATE, -1);//日期回滚一天，也就是最后一天
        int maxDate = a.get(Calendar.DATE);
        return maxDate;
    }


    public static String timeDifferent(long begin, long end) {
        if (begin > end) {
            return timeDifferent(end, begin);
        }
        long diff = end - begin;

        long day = diff / DAY_MILLION_SECONDS;
        diff = diff - DAY_MILLION_SECONDS * day;
        long hour = diff / HOUR_MILLION_SECONDS;
        diff = diff - hour * HOUR_MILLION_SECONDS;
        long minute = diff / MINUTE_MILLION_SECONDS;
        diff = diff - minute * MINUTE_MILLION_SECONDS;
        long second = diff / MILLOIN_SECONDS;
        StringBuilder timeDiff = new StringBuilder();
        if (day > 0) {
            timeDiff.append(day + "天");
        }
        if (hour > 0) {
            timeDiff.append(hour + "小时");
        }
        if (minute > 0) {
            timeDiff.append(minute + "分");
        }
        if (second > 0) {
            timeDiff.append(second + "秒");
        }

        return timeDiff.toString();
    }


    //找到前n周的date，如果n是负数就是找后N周
    public static String getPreNWeek(final Date date, int n) {
        if (date == null) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.set(Calendar.WEEK_OF_YEAR, calendar.get(Calendar.WEEK_OF_YEAR) - n);
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        return df.format(calendar.getTime());
    }

    //找到前n月的date，如果n是负数就是找后N月
    public static String getPreNMonth(final Date date, int n) {
        if (date == null) {
            return null;
        }
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.set(Calendar.MONTH, calendar.get(Calendar.MONTH) - n);
        SimpleDateFormat df = new SimpleDateFormat("yyyy-MM-dd");
        return df.format(calendar.getTime());
    }

    /**
     * 获取最近num 个月的日期  返回格式为formate
     *
     * @param num
     * @param formate
     * @return
     */
    public static List<String> getLastMonthList(int num, String formate) {
        Date now = new Date();
        List<String> monthStrList = new ArrayList<>(12);
        while (num > 0) {
            num--;
            Date before = stepMonth(now, -num);
            monthStrList.add(convertDateToDateString(formate, before));
        }

        return monthStrList;
    }

    public static Date stepMonth(Date sourceDate, int month) {
        Calendar c = Calendar.getInstance();
        c.setTime(sourceDate);
        c.add(Calendar.MONTH, month);

        return c.getTime();
    }


    /**
     * 获取每周的第一天
     *
     * @return 时间
     */
    public static Date getFirstDayOfWeek() {
        Calendar calendar = Calendar.getInstance();
        calendar.add(Calendar.DATE, -1);
        calendar.set(Calendar.DAY_OF_WEEK, Calendar.SATURDAY);
        calendar.add(Calendar.DATE, -5);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return (Date) calendar.getTime().clone();
    }

    /**
     * 获得时间所在月的最后一天
     */
    public static Long getLastDayOfMonth(Date date) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.add(Calendar.MONTH, 1);
        calendar.set(Calendar.DATE, 1);
        calendar.add(Calendar.DATE, -1);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return calendar.getTimeInMillis();
    }

    /**
     * 获得时间所在周的最后一天
     */
    public static Long getLastDayOfWeek(Date date) {
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.setFirstDayOfWeek(Calendar.MONDAY);
        calendar.set(Calendar.DAY_OF_WEEK, Calendar.SUNDAY);
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        return calendar.getTimeInMillis();
    }

    /**
     * 取得目前几号
     */
//    public static int getCurrentDay()
//    {
//        Calendar a = Calendar.getInstance();
//        int d=a.get(Calendar.DATE);
//        return d;
//    }
    public static void main(String[] args) {
  /*      Long time = System.currentTimeMillis();
        Calendar cal = Calendar.getInstance();
        cal.setTime(new Date(time));
        cal.set(Calendar.MONTH, cal.get(Calendar.MONTH) - 0);
        int day = cal.get(Calendar.DAY_OF_MONTH);*/
        //
       /* Long time = convertDateStringToDateLong(DATE_STRING_FORMAT_DAY,"2019-09-11");
        */

//        Long time2 =  geFirstDayOfLastMonth();
//        Long time3 = getLastDayOfLastMonth();
//
//
//
//
//        double s = Double.parseDouble(DateUtil.getCurrentDay()+"")/Double.parseDouble(DateUtil.getCurrentMonthLastDay()+"")*100;
//        BigDecimal b = new BigDecimal(s);
//        double df = b.setScale(2, BigDecimal.ROUND_HALF_UP).doubleValue();
//
        List<String> monthList = getLastMonthList(12, "yy-MM");
        for (String s : monthList) {

        }
    }

    /**
     * 返回指定的时间段内的有所日期
     *
     * @param startDate 开始时间,例:2016-02-25
     * @param endDate   结束时间,例:2016-03-05
     * @return 天数
     */
    public static String[] dateSplit(String startDate, String endDate) {
        // 开始时间必须小于等于结束时间
        if (DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, startDate) > DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, endDate)) {
            throw new EcovacsValidateException("开始时间必须小于等于结束时间");
        }
        String[] ar = {};
        SimpleDateFormat simpleDateFormat = new SimpleDateFormat("yyyy-MM-dd");
        Date newStartDate;
        try {
            newStartDate = simpleDateFormat.parse(startDate);
            Date newendDate = simpleDateFormat.parse(endDate);
            List<String> dateList = new ArrayList<>();
            long spi = newendDate.getTime() - newStartDate.getTime();
            long step = spi / (24 * 60 * 60 * 1000);// 相隔天数
            List<Date> newDateList = new ArrayList<>();
            SimpleDateFormat formatter = new SimpleDateFormat("yyyy-MM-dd");
            newDateList.add(newStartDate);
            dateList.add(startDate);
            for (int i = 1; i <= step; i++) {
                dateList.add(formatter.format(new Date(newDateList.get(i - 1).getTime() + (24 * 60 * 60 * 1000))));
                newDateList.add(new Date(newDateList.get(i - 1).getTime() + (24 * 60 * 60 * 1000)));// 比上一天+一
            }
            String[] strings = new String[dateList.size()];
            return dateList.toArray(strings);
        } catch (ParseException e) {
            e.printStackTrace();
            return ar;
        }
    }

    /**
     * 获取当前时间的整点小时时间
     */
    public static Date currHourTimeLong() {
        Calendar cal = Calendar.getInstance();
        cal.set(Calendar.MINUTE, 0);
        cal.set(Calendar.SECOND, 0);
        return cal.getTime();
    }

    /**
     * 根据传入的2019-01-01，2019-03-02 返回 2019年1月 2019年2月 2019年3月
     *
     * @param startDate
     * @param endDate
     * @return
     */
    public static List<String> getMonthsYearMonth(String startDate, String endDate) {
        // 开始时间必须小于等于结束时间
        if (DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, startDate) > DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, endDate)) {
            throw new EcovacsValidateException("开始时间必须小于等于结束时间");
        }
        SimpleDateFormat simpleDateFormat = new SimpleDateFormat("yyyy-MM-dd");
        SimpleDateFormat monthFormat = new SimpleDateFormat(DateUtil.YEAR_MONTH);
        List<Long> times = new ArrayList<>();
        List<String> timeList = new ArrayList<>();
        Date newStartDate;
        try {
            newStartDate = simpleDateFormat.parse(startDate);
            Calendar can = Calendar.getInstance();
            can.setTime(newStartDate);
            timeList.add(monthFormat.format(newStartDate));
            Date newendDate = simpleDateFormat.parse(endDate);
            Calendar canEnd = Calendar.getInstance();
            canEnd.setTime(newendDate);
            while (can.compareTo(canEnd) < 0) {
                can.add(Calendar.MONTH, 1);
                if (can.compareTo(canEnd) < 0) {
                    times.add(can.getTime().getTime());
                }
            }
            Arrays.sort(times.toArray());
            for (Long time : times) {
                timeList.add(monthFormat.format(new Date(time)));
            }
        } catch (ParseException e) {
            e.printStackTrace();

        }
        return  timeList;
    }


    /**
     * 根据传入的2019-01-01，2019-03-02 返回 2019年1月 2019年2月 2019年3月
     *
     * @param startDate
     * @param endDate
     * @return
     */
    public static List<String> getMonths(String startDate, String endDate) {
        // 开始时间必须小于等于结束时间
        if (DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, startDate) > DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, endDate)) {
            throw new EcovacsValidateException("开始时间必须小于等于结束时间");
        }
        SimpleDateFormat simpleDateFormat = new SimpleDateFormat("yyyy-MM-dd");
        SimpleDateFormat monthFormat = new SimpleDateFormat(DateUtil.DATE_STRING_FORMAT_CN2);
        List<Long> times = new ArrayList<>();
        List<String> timeList = new ArrayList<>();
        Date newStartDate;
        try {
            newStartDate = simpleDateFormat.parse(startDate);
            Calendar can = Calendar.getInstance();
            can.setTime(newStartDate);
            timeList.add(monthFormat.format(newStartDate));
            Date newendDate = simpleDateFormat.parse(endDate);
            Calendar canEnd = Calendar.getInstance();
            canEnd.setTime(newendDate);
            while (can.compareTo(canEnd) < 0) {
                can.add(Calendar.MONTH, 1);
                if (can.compareTo(canEnd) < 0) {
                    times.add(can.getTime().getTime());
                }
            }
            Arrays.sort(times.toArray());
            for (Long time : times) {
                timeList.add(monthFormat.format(new Date(time)));
            }
        } catch (ParseException e) {
            e.printStackTrace();

        }
        return  timeList;
    }


    public static Long convertStringToLongValidateEx(String format, String time)  {
        if (SysUtil.isEmpty(time)) {
            return null;
        }
        SimpleDateFormat fmt = new SimpleDateFormat(format);
        Date d = null;
        try {
            d = fmt.parse(time);
        } catch (ParseException e) {
            throw new EcovacsValidateException(String.format("[%s]时间格式不符合[%s]",time,format));
        }
        return d == null ? null : d.getTime();
    }


    /**
     * 根据传入的2019-01-01，2019-03-02 返回 2019年1月 2019年2月 2019年3月
     *
     * @param startDate
     * @param endDate
     * @return
     */
    public static List<String> getMonthsBetween(String startDate, String endDate, String format) {
        // 开始时间必须小于等于结束时间
        if (DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, startDate) > DateUtil.convertDateStringToDateLong(DateUtil.YEAR_MONTH_DATE, endDate)) {
            throw new EcovacsValidateException("开始时间必须小于等于结束时间");
        }
        ArrayList<String> result = new ArrayList<String>();
        SimpleDateFormat sdf = new SimpleDateFormat(YEAR_MONTH_DATE);//格式化为年月
        SimpleDateFormat sdf2 = new SimpleDateFormat(format);//格式化为年月

        Calendar min = Calendar.getInstance();
        Calendar max = Calendar.getInstance();
        Date start = null;
        try {
            start = sdf.parse(startDate);
            min.setTime(start);
            min.set(min.get(Calendar.YEAR), min.get(Calendar.MONTH), 1);
            Date end = sdf.parse(endDate);
            max.setTime(end);
            max.set(max.get(Calendar.YEAR), max.get(Calendar.MONTH), 2);

            Calendar curr = min;
            while (curr.before(max)) {
                result.add(sdf2.format(curr.getTime()));
                curr.add(Calendar.MONTH, 1);
            }
        }
        catch (ParseException e) {
            e.printStackTrace();
        }
        return result;
    }

     public static List<String> get6Month(String time) {
        List<String> list = new ArrayList<>();
        SimpleDateFormat sdf = new SimpleDateFormat(YEAR_MONTH);
         Date dateStart = new Date();
         try {
             dateStart = sdf.parse(time);
         } catch (ParseException e) {
             e.printStackTrace();
         }

         for (int i = 6; i > 0; i--) {
            Calendar calendar = Calendar.getInstance();
            //获取当前时间的前6个月
            calendar.setTime(dateStart);
            calendar.add(Calendar.MONTH,-i);
            //将calendar装换为Date类型
            Date date = calendar.getTime();

            list.add(sdf.format(date));
        }
        return list;
    }

    public static Date getMonthEndTime(Date date){
        Calendar c = Calendar.getInstance();
        c.setTime(date);
        c.set(Calendar.DAY_OF_MONTH,c.getActualMaximum(Calendar.DAY_OF_MONTH));//设置为最后一天
        c.set(Calendar.HOUR_OF_DAY, 23);
        c.set(Calendar.MINUTE, 59);
        c.set(Calendar.SECOND, 59);
        c.set(Calendar.MILLISECOND, 999);
        return c.getTime();
    }


    public static boolean getTimeBetweenTimeSection (String time, int startSection, int endSection){
        boolean flag = false;

        Long timeLong = DateUtil.convertDateStringToDateLong(DateUtil.ALL, time);

        Calendar c = Calendar.getInstance();
        c.setTimeInMillis(timeLong);


        Calendar min=Calendar.getInstance();
        //min=currentDate;
        min.setTimeInMillis(timeLong);
        min.set(Calendar.YEAR, c.get(Calendar.YEAR));
        min.set(Calendar.MONTH, c.get(Calendar.MONTH));
        min.set(Calendar.HOUR_OF_DAY, startSection);
        min.set(Calendar.MINUTE, 0);
        min.set(Calendar.SECOND, 0);
        min.set(Calendar.MILLISECOND, 0);

        Calendar max=Calendar.getInstance();
        // max=currentDate;
        max.setTimeInMillis(timeLong);
        max.set(Calendar.YEAR, c.get(Calendar.YEAR));
        max.set(Calendar.MONTH, c.get(Calendar.MONTH));
        max.set(Calendar.HOUR_OF_DAY, endSection);
        max.set(Calendar.MINUTE, 0);
        max.set(Calendar.SECOND, 0);
        max.set(Calendar.MILLISECOND, 0);

        if(c.getTimeInMillis()>=min.getTimeInMillis() && c.getTimeInMillis()<=max.getTimeInMillis()){
            flag = true;
        }
        return flag;

        }

    public static boolean isFormatDate(String format, String timeStr) {
        SimpleDateFormat sdf = new SimpleDateFormat(format);
        sdf.setLenient(false);  // 严格匹配格式，不容忍错误
        try {
            // 尝试解析字符串为日期
            Date date = sdf.parse(timeStr);
            return true;
        } catch (ParseException e) {
            // 解析失败，说明不满足时间格式
            return false;
        }
    }

    //本方法仅供门店app首页周趋势图使用, 其他地方千万别用
    public static List<String> getWeekDay4Week(String input) {
        List<String> weekDays = new ArrayList<>();
        String[] split = input.split("W");
        String yearStr = split[0];
        String weekStr = split[1];
        int year = 2000 + Integer.parseInt(yearStr); // 输入的年份只能是从2000年开始的
        int week = Integer.parseInt(weekStr);
        week -= 1;
        Calendar calendar = Calendar.getInstance();
        calendar.clear();
        calendar.set(Calendar.YEAR, year);
        calendar.set(Calendar.WEEK_OF_YEAR, week);
        calendar.set(Calendar.DAY_OF_WEEK, Calendar.MONDAY); // 设置为周一

        SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd");
        String startDate = dateFormat.format(calendar.getTime());

        calendar.add(Calendar.DAY_OF_WEEK, 6);  // 将日期加6天，得到周日
        String endDate = dateFormat.format(calendar.getTime());
        weekDays.add(startDate);
        weekDays.add(endDate);
        return weekDays;
    }

    //本方法仅供门店app首页周趋势图使用, 其他地方千万别用
    public static List<String> getWeekDay2Week(String input) {
        List<String> weekDays = new ArrayList<>();
        String[] split = input.split("W");
        String yearStr = split[0];
        String weekStr = split[1];
        int year = 2000 + Integer.parseInt(yearStr); // 输入的年份只能是从2000年开始的
        int week = Integer.parseInt(weekStr);
        if (year < 2024) week -= 1;
        Calendar calendar = Calendar.getInstance();
        calendar.clear();
        calendar.set(Calendar.YEAR, year);
        calendar.set(Calendar.WEEK_OF_YEAR, week);
        calendar.set(Calendar.DAY_OF_WEEK, Calendar.MONDAY); // 设置为周一

        SimpleDateFormat dateFormat = new SimpleDateFormat("yyyy-MM-dd");
        String startDate = dateFormat.format(calendar.getTime());

        calendar.add(Calendar.DAY_OF_WEEK, 6);  // 将日期加6天，得到周日
        String endDate = dateFormat.format(calendar.getTime());
        weekDays.add(startDate);
        weekDays.add(endDate);
        return weekDays;
    }

    public static String getYearWeekData(String format, String dates) {
        Long date = DateUtil.convertDateStringToDateLong(format, dates);
        Calendar cal = Calendar.getInstance();
        cal.setTimeInMillis(date);//设置为指定时间
        cal.setFirstDayOfWeek(Calendar.MONDAY);

        String year = cal.get(Calendar.YEAR) + StringUtils.EMPTY;
        int week = cal.get(Calendar.WEEK_OF_YEAR);

        return year.substring(2) + "W" + week;
    }

    public static List<String> getMonthDay2Month(String input) {
        List<String> monthDays = new ArrayList<>();
        String month = "20" + input + "-01";
        Long startTimeOfMonthStr = getStartTimeOfMonthStr(month, YEAR_MONTH_DATE);
        Long endTimeOfMonthStr = getEndTimeOfMonthStr(month, YEAR_MONTH_DATE);
        monthDays.add(convertDateLongToDateString(YEAR_MONTH_DATE, startTimeOfMonthStr));
        monthDays.add(convertDateLongToDateString(YEAR_MONTH_DATE, endTimeOfMonthStr));
        return monthDays;
    }

    public static  long getStartOfDay(){
        Calendar calendar = Calendar.getInstance();

        // 将日期减去一天
        calendar.add(Calendar.DAY_OF_YEAR, -1);

        // 设置时间为00:00:00
        calendar.set(Calendar.HOUR_OF_DAY, 0);
        calendar.set(Calendar.MINUTE, 0);
        calendar.set(Calendar.SECOND, 0);
        calendar.set(Calendar.MILLISECOND, 0);
        long startOfDay = calendar.getTimeInMillis();
        return calendar.getTimeInMillis();
    }

    public static  long getEndOfDay(){
        Calendar calendar = Calendar.getInstance();
        // 将日期减去一天
        calendar.add(Calendar.DAY_OF_YEAR, -1);
        // 设置时间为23:59:59
        calendar.set(Calendar.HOUR_OF_DAY, 23);
        calendar.set(Calendar.MINUTE, 59);
        calendar.set(Calendar.SECOND, 59);
        calendar.set(Calendar.MILLISECOND, 999);

        return calendar.getTimeInMillis();
    }
}

```

