// 文件名：FileManager.cs
using System;
using System.IO;
using System.Windows.Forms;
using System.Drawing;
using System.Text;
using System.Collections.Generic;
using System.Diagnostics;
using System.Globalization;

// 本地文件管理器主窗口
public class FileManagerForm : Form
{
    private TreeView treeViewDirectory;
    private Button btnSelectFolder;
    private Button btnGit;
    private Button btnRefresh;
    private Button btnSearch; // 新增的搜索按钮
    private Button btnTools; // 新增的工具按钮
    private ContextMenuStrip contextMenu;
    private ToolStripMenuItem menuOpenFolder;
    private string rootFolderPath = string.Empty;
    private string configFilePath;
    private Dictionary<string, Color> fileTypeColorMap;
    private List<TreeNode> highlightedNodes = new List<TreeNode>(); // 存储高亮节点
    private List<TreeNode> contentHighlightedNodes = new List<TreeNode>(); // 存储内容高亮节点

    // 新增：剪贴板相关变量
    private string copiedItemPath = null; // 存储复制/剪切的文件/文件夹路径
    private bool isCut = false; // 是否是剪切操作（默认false）

    // 新增：通知标签和定时器（用于自动隐藏提示）
    private Label lblNotification;
    private Timer notificationTimer;

    public FileManagerForm()
    {
        configFilePath = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "config.ini");
        InitFileTypeColorMap();
        this.Text = "RootTrees";
        SetWindowSizeBySystemScreen();
        this.StartPosition = FormStartPosition.Manual;
        this.Location = new Point(100, 0);
        this.FormBorderStyle = FormBorderStyle.Sizable;
        this.KeyPreview = true;
        this.Icon = Icon.ExtractAssociatedIcon(Application.ExecutablePath);

        // 初始化通知标签和定时器
        InitNotificationControl();

        treeViewDirectory = new TreeView();
        treeViewDirectory.Dock = DockStyle.Fill;
        treeViewDirectory.Font = new Font("微软雅黑", 10);
        treeViewDirectory.ShowLines = true;
        treeViewDirectory.ShowPlusMinus = true;
        treeViewDirectory.ShowRootLines = true;
        treeViewDirectory.DrawMode = TreeViewDrawMode.OwnerDrawText;
        treeViewDirectory.DrawNode += TreeViewDirectory_DrawNode;
        treeViewDirectory.NodeMouseDoubleClick += TreeViewDirectory_NodeMouseDoubleClick;
        treeViewDirectory.MouseClick += TreeViewDirectory_MouseClick;
        treeViewDirectory.NodeMouseClick += TreeViewDirectory_NodeMouseClick;
        // 注册TreeView的按键事件（处理Delete键）
        treeViewDirectory.KeyDown += TreeViewDirectory_KeyDown;
        this.Controls.Add(treeViewDirectory);

        btnSelectFolder = new Button();
        btnSelectFolder.Text = "选择根目录";
        btnSelectFolder.Size = new Size(120, 30);
        btnSelectFolder.Font = new Font("微软雅黑", 9);
        btnSelectFolder.Click += BtnSelectFolder_Click;

        btnGit = new Button();
        btnGit.Text = "git";
        btnGit.Size = new Size(60, 30);
        btnGit.Font = new Font("微软雅黑", 9);
        btnGit.Click += BtnGit_Click;

        btnRefresh = new Button();
        btnRefresh.Text = "刷新";
        btnRefresh.Size = new Size(60, 30);
        btnRefresh.Font = new Font("微软雅黑", 9);
        btnRefresh.Click += BtnRefresh_Click;

        btnSearch = new Button();
        btnSearch.Text = "搜索";
        btnSearch.Size = new Size(60, 30);
        btnSearch.Font = new Font("微软雅黑", 9);
        btnSearch.Click += BtnSearch_Click;

        // 新增：工具按钮
        btnTools = new Button();
        btnTools.Text = "工具";
        btnTools.Size = new Size(60, 30);
        btnTools.Font = new Font("微软雅黑", 9);
        btnTools.Click += BtnTools_Click;

        FlowLayoutPanel buttonPanel = new FlowLayoutPanel();
        buttonPanel.Dock = DockStyle.Top;
        buttonPanel.AutoSize = true;
        buttonPanel.Controls.Add(btnSelectFolder);
        buttonPanel.Controls.Add(btnGit);
        buttonPanel.Controls.Add(btnRefresh);
        buttonPanel.Controls.Add(btnSearch);
        buttonPanel.Controls.Add(btnTools); // 添加工具按钮到面板
        this.Controls.Add(buttonPanel);

        // 将通知标签添加到窗体，置于最顶层
        this.Controls.Add(lblNotification);
        this.Controls.SetChildIndex(lblNotification, 0); // 确保标签在最上层显示

        this.Padding = new Padding(10);
        InitContextMenu();
        LoadConfigAndInitDirectory();
    }

    // 新增：工具按钮点击事件
    private void BtnTools_Click(object sender, EventArgs e)
    {
        ContextMenuStrip toolsMenu = new ContextMenuStrip();
        toolsMenu.Font = new Font("微软雅黑", 9);

        // RootTrees选项（当前页面）
        ToolStripMenuItem menuRootTrees = new ToolStripMenuItem("RootTrees");
        menuRootTrees.Click += (s, ev) =>
        {
            // 激活当前窗体（因为当前已经是RootTrees页面，所以只需聚焦）
            this.Activate();
        };
        toolsMenu.Items.Add(menuRootTrees);

        // 时间戳转换选项
        ToolStripMenuItem menuTimestampConverter = new ToolStripMenuItem("时间戳转换");
        menuTimestampConverter.Click += (s, ev) =>
        {
            // 打开时间戳转换窗体
            using (TimestampConverterForm converterForm = new TimestampConverterForm())
            {
                converterForm.ShowDialog(this);
            }
        };
        toolsMenu.Items.Add(menuTimestampConverter);

        // 显示下拉菜单
        toolsMenu.Show(btnTools, new Point(0, btnTools.Height));
    }

    // 新增：初始化通知控件
    private void InitNotificationControl()
    {
        // 创建通知标签
        lblNotification = new Label();
        lblNotification.Text = string.Empty;
        lblNotification.Font = new Font("微软雅黑", 9);
        lblNotification.ForeColor = Color.Green;
        lblNotification.BackColor = Color.FromArgb(240, 248, 255); // 淡蓝色背景
        lblNotification.BorderStyle = BorderStyle.FixedSingle;
        lblNotification.Padding = new Padding(5);
        lblNotification.AutoSize = true;
        // 定位到窗体右下角
        lblNotification.Location = new Point(this.ClientSize.Width - 200, this.ClientSize.Height - 50);
        lblNotification.Visible = false; // 初始隐藏

        // 创建定时器，用于自动隐藏通知（2秒后隐藏）
        notificationTimer = new Timer();
        notificationTimer.Interval = 2000; // 2000毫秒 = 2秒
        notificationTimer.Tick += (s, e) =>
        {
            lblNotification.Visible = false;
            notificationTimer.Stop();
        };

        // 监听窗体大小变化，调整通知标签位置
        this.Resize += (s, e) =>
        {
            lblNotification.Location = new Point(this.ClientSize.Width - lblNotification.Width - 20, this.ClientSize.Height - lblNotification.Height - 20);
        };
    }

    // 新增：显示通知的方法
    private void ShowNotification(string message, Color textColor = default(Color))
    {
        if (textColor == default(Color))
        {
            textColor = Color.Green; // 默认绿色提示
        }
        lblNotification.Text = message;
        lblNotification.ForeColor = textColor;
        // 调整标签位置到窗体右下角（预留边距）
        lblNotification.Location = new Point(this.ClientSize.Width - lblNotification.Width - 20, this.ClientSize.Height - lblNotification.Height - 20);
        lblNotification.Visible = true;
        // 启动定时器，2秒后隐藏
        notificationTimer.Stop();
        notificationTimer.Start();
    }

    private void BtnRefresh_Click(object sender, EventArgs e)
    {
        RefreshTreeView();
    }

    private void BtnGit_Click(object sender, EventArgs e)
    {
        if (string.IsNullOrEmpty(rootFolderPath))
        {
            MessageBox.Show("请先选择根目录！", "提示", MessageBoxButtons.OK, MessageBoxIcon.Information);
            return;
        }

        ContextMenuStrip gitMenu = new ContextMenuStrip();
        gitMenu.Font = new Font("微软雅黑", 9);

        ToolStripMenuItem menuOpenConsole = new ToolStripMenuItem("打开控制台");
        menuOpenConsole.Click += (s, ev) => OpenConsole();
        gitMenu.Items.Add(menuOpenConsole);

        ToolStripMenuItem menuGitPull = new ToolStripMenuItem("Git Pull");
        menuGitPull.Click += (s, ev) => ExecuteGitCommand("git pull");
        gitMenu.Items.Add(menuGitPull);

        ToolStripMenuItem menuGitPush = new ToolStripMenuItem("Git Push");
        menuGitPush.Click += (s, ev) => GitPush();
        gitMenu.Items.Add(menuGitPush);

        gitMenu.Show(btnGit, new Point(0, btnGit.Height));
    }

    private void OpenConsole()
    {
        // 路径包裹双引号，处理特殊字符
        ProcessStartInfo psi = new ProcessStartInfo("cmd.exe", string.Format("/K cd /d \"{0}\"", rootFolderPath));
        Process.Start(psi);
    }

    private void ExecuteGitCommand(string command)
    {
        // 路径包裹双引号，处理特殊字符
        ProcessStartInfo psi = new ProcessStartInfo("cmd.exe", string.Format("/C cd /d \"{0}\" && {1}", rootFolderPath, command));
        psi.UseShellExecute = false;
        psi.RedirectStandardOutput = true;
        psi.RedirectStandardError = true;
        psi.CreateNoWindow = true;

        using (Process process = Process.Start(psi))
        {
            string output = process.StandardOutput.ReadToEnd();
            string error = process.StandardError.ReadToEnd();
            process.WaitForExit();

            if (!string.IsNullOrEmpty(output))
            {
                MessageBox.Show(output, "Git Output", MessageBoxButtons.OK, MessageBoxIcon.Information);
            }
            if (!string.IsNullOrEmpty(error))
            {
                MessageBox.Show(error, "Git Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
            }
        }
    }

    private void GitPush()
    {
        string commitMessage = PromptForCommitMessage();
        if (string.IsNullOrEmpty(commitMessage))
        {
            return;
        }

        ExecuteGitCommand("git add .");
        ExecuteGitCommand(string.Format("git commit -m \"{0}\"", commitMessage));
        ExecuteGitCommand("git push");
    }

    private string PromptForCommitMessage()
    {
        using (Form prompt = new Form())
        {
            prompt.Width = 300;
            prompt.Height = 150;
            prompt.Text = "Git Commit Message";
            prompt.StartPosition = FormStartPosition.CenterParent;

            Label textLabel = new Label() { Left = 10, Top = 20, Text = "请输入提交信息：" };
            TextBox textBox = new TextBox() { Left = 10, Top = 50, Width = 260 };

            Button confirmation = new Button() { Text = "确定", Left = 200, Width = 70, Top = 80, DialogResult = DialogResult.OK };
            confirmation.Click += (sender, e) => { prompt.Close(); };

            prompt.Controls.Add(textLabel);
            prompt.Controls.Add(textBox);
            prompt.Controls.Add(confirmation);
            prompt.AcceptButton = confirmation;

            return prompt.ShowDialog(this) == DialogResult.OK ? textBox.Text : null;
        }
    }

    private void ShowGitCommands()
    {
        Form gitCommandForm = new Form();
        gitCommandForm.Text = "Git 命令提示";
        gitCommandForm.StartPosition = FormStartPosition.CenterParent;
        gitCommandForm.Size = new Size(400, 300);

        Label gitCommandsLabel = new Label();
        gitCommandsLabel.Text = "常用 Git 命令:\n" +
                                "1. git init - 初始化仓库\n" +
                                "2. git status - 查看状态\n" +
                                "3. git add . - 添加所有更改\n" +
                                "4. git commit -m \"message\" - 提交更改\n" +
                                "5. git push - 推送到远程仓库\n" +
                                "6. git pull - 拉取最新更改\n" +
                                "7. git clone <url> - 克隆仓库";
        gitCommandsLabel.Dock = DockStyle.Fill;
        gitCommandsLabel.TextAlign = ContentAlignment.MiddleLeft;
        gitCommandsLabel.Padding = new Padding(10);

        gitCommandForm.Controls.Add(gitCommandsLabel);
        gitCommandForm.ShowDialog(this);
    }

    private void SetWindowSizeBySystemScreen()
    {
        Rectangle screenWorkingArea = Screen.PrimaryScreen.WorkingArea;
        int windowWidth = (int)(screenWorkingArea.Width * 0.25);
        int windowHeight = (int)(screenWorkingArea.Height * 1);
        this.Size = new Size(windowWidth, windowHeight);
        this.MinimumSize = new Size(400, 500);
        this.PerformLayout();
    }

    // 重写快捷键处理方法，新增Ctrl+C、Ctrl+X、Ctrl+V处理
    protected override bool ProcessCmdKey(ref Message msg, Keys keyData)
    {
        if (keyData == Keys.Escape)
        {
            this.WindowState = FormWindowState.Minimized;
            return true;
        }
        // 处理Ctrl+C复制
        else if (keyData == (Keys.Control | Keys.C))
        {
            CopyItem();
            return true;
        }
        // 处理Ctrl+X剪切
        else if (keyData == (Keys.Control | Keys.X))
        {
            CutItem();
            return true;
        }
        // 处理Ctrl+V粘贴
        else if (keyData == (Keys.Control | Keys.V))
        {
            PasteItem();
            return true;
        }
        return base.ProcessCmdKey(ref msg, keyData);
    }

    // 处理TreeView的按键事件（Delete键）
    private void TreeViewDirectory_KeyDown(object sender, KeyEventArgs e)
    {
        if (e.KeyCode == Keys.Delete)
        {
            // 调用删除方法，和右键删除走同一逻辑
            MenuDelete_Click(sender, e);
            e.Handled = true; // 标记事件已处理，避免后续传递
        }
    }

    // 复制选中的文件/文件夹
    private void CopyItem()
    {
        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null || selectedNode.Tag == null)
        {
            ShowNotification("请选择要复制的文件或文件夹！", Color.Orange); // 橙色提示警告
            return;
        }

        string path = selectedNode.Tag.ToString();
        if (File.Exists(path) || Directory.Exists(path))
        {
            copiedItemPath = path;
            isCut = false; // 复制操作，剪切标记置为false
            // 也可以将路径写入系统剪贴板，增强兼容性
            Clipboard.SetText(path);
            ShowNotification(string.Format("已复制：{0}", path)); // 绿色提示成功
        }
        else
        {
            ShowNotification("选中的项不是有效的文件或文件夹！", Color.Orange);
        }
    }

    // 剪切选中的文件/文件夹
    private void CutItem()
    {
        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null || selectedNode.Tag == null)
        {
            ShowNotification("请选择要剪切的文件或文件夹！", Color.Orange);
            return;
        }

        string path = selectedNode.Tag.ToString();
        if (File.Exists(path) || Directory.Exists(path))
        {
            copiedItemPath = path;
            isCut = true; // 剪切操作，剪切标记置为true
            Clipboard.SetText(path);
            ShowNotification(string.Format("已剪切：{0}", path));
        }
        else
        {
            ShowNotification("选中的项不是有效的文件或文件夹！", Color.Orange);
        }
    }

    // 粘贴文件/文件夹到选中的文件夹
    private void PasteItem()
    {
        // 检查是否有复制/剪切的内容
        if (string.IsNullOrEmpty(copiedItemPath) && string.IsNullOrEmpty(Clipboard.GetText()))
        {
            ShowNotification("没有可粘贴的内容！", Color.Orange);
            return;
        }

        // 优先使用本地变量，没有则从系统剪贴板获取
        string sourcePath = !string.IsNullOrEmpty(copiedItemPath) ? copiedItemPath : Clipboard.GetText();
        if (!File.Exists(sourcePath) && !Directory.Exists(sourcePath))
        {
            ShowNotification("剪贴板中的内容不是有效的文件或文件夹路径！", Color.Orange);
            return;
        }

        // 检查选中的目标位置
        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null || selectedNode.Tag == null)
        {
            ShowNotification("请选择要粘贴到的目标文件夹！", Color.Orange);
            return;
        }

        string targetPath = selectedNode.Tag.ToString();
        // 如果选中的是文件，则取其所在目录作为目标路径
        if (File.Exists(targetPath))
        {
            targetPath = Path.GetDirectoryName(targetPath);
        }
        // 确保目标路径是文件夹
        if (!Directory.Exists(targetPath))
        {
            ShowNotification("目标位置不是有效的文件夹！", Color.Orange);
            return;
        }

        // 防止粘贴到自身目录（文件夹）
        if (Directory.Exists(sourcePath) && targetPath.StartsWith(sourcePath, StringComparison.OrdinalIgnoreCase))
        {
            ShowNotification("无法将文件夹粘贴到自身目录中！", Color.Orange);
            return;
        }

        // 构建目标文件/文件夹的路径
        string itemName = Path.GetFileName(sourcePath);
        string destPath = Path.Combine(targetPath, itemName);

        // 处理重名问题
        destPath = GetUniquePath(destPath);

        try
        {
            if (File.Exists(sourcePath))
            {
                // 复制文件
                File.Copy(sourcePath, destPath, false); // false：不覆盖已存在的文件
                // 如果是剪切操作，复制后删除原文件
                if (isCut)
                {
                    File.Delete(sourcePath);
                    ShowNotification(string.Format("文件已剪切到：{0}", destPath));
                }
                else
                {
                    ShowNotification(string.Format("文件已粘贴到：{0}", destPath));
                }
            }
            else if (Directory.Exists(sourcePath))
            {
                // 复制文件夹（递归复制所有内容）
                CopyDirectory(sourcePath, destPath);
                // 如果是剪切操作，复制后删除原文件夹
                if (isCut)
                {
                    Directory.Delete(sourcePath, true);
                    ShowNotification(string.Format("文件夹已剪切到：{0}", destPath));
                }
                else
                {
                    ShowNotification(string.Format("文件夹已粘贴到：{0}", destPath));
                }
            }

            // 记录目标路径的父级，刷新后定位
            string parentPath = Path.GetDirectoryName(destPath);
            RefreshTreeView(parentPath);
            SelectFileInTreeView(destPath);

            // 剪切操作完成后，重置剪切标记（可选，避免重复剪切）
            if (isCut)
            {
                isCut = false;
                copiedItemPath = null;
            }
        }
        catch (UnauthorizedAccessException)
        {
            ShowNotification("没有权限执行粘贴操作！", Color.Red); // 红色提示错误
        }
        catch (Exception ex)
        {
            ShowNotification(string.Format("粘贴失败：{0}", ex.Message), Color.Red);
        }
    }

    // 获取唯一的路径（处理重名，自动加数字后缀）
    private string GetUniquePath(string path)
    {
        if (!File.Exists(path) && !Directory.Exists(path))
        {
            return path;
        }

        string directory = Path.GetDirectoryName(path);
        string fileName = Path.GetFileNameWithoutExtension(path);
        string extension = Path.GetExtension(path);
        int index = 1;

        // 循环直到找到不重复的路径
        do
        {
            string newName = string.Format("{0}({1}){2}", fileName, index, extension);
            path = Path.Combine(directory, newName);
            index++;
        } while (File.Exists(path) || Directory.Exists(path));

        return path;
    }

    // 递归复制文件夹
    private void CopyDirectory(string sourceDir, string destDir)
    {
        // 创建目标文件夹
        Directory.CreateDirectory(destDir);

        // 复制文件夹中的文件
        foreach (string file in Directory.GetFiles(sourceDir))
        {
            string destFile = Path.Combine(destDir, Path.GetFileName(file));
            File.Copy(file, destFile, false);
        }

        // 递归复制子文件夹
        foreach (string subDir in Directory.GetDirectories(sourceDir))
        {
            string destSubDir = Path.Combine(destDir, Path.GetFileName(subDir));
            CopyDirectory(subDir, destSubDir);
        }
    }

    private void InitFileTypeColorMap()
    {
        fileTypeColorMap = new Dictionary<string, Color>(StringComparer.OrdinalIgnoreCase)
        {
            { ".txt", Color.Black },
            { ".doc", Color.DarkBlue },
            { ".docx", Color.DarkBlue },
            { ".pdf", Color.DarkRed },
            { ".xls", Color.DarkGreen },
            { ".xlsx", Color.DarkGreen },
            { ".ppt", Color.Orange },
            { ".pptx", Color.Orange },
            { ".md", Color.Purple },
            { ".cs", Color.Blue },
            { ".java", Color.Brown },
            { ".py", Color.Green },
            { ".js", Color.DarkGoldenrod },
            { ".html", Color.OrangeRed },
            { ".css", Color.DeepSkyBlue },
            { ".sql", Color.DarkCyan },
            { ".jpg", Color.Olive },
            { ".png", Color.Olive },
            { ".gif", Color.Olive },
            { ".bmp", Color.Olive },
            { ".svg", Color.Olive },
            { ".zip", Color.DarkMagenta },
            { ".rar", Color.DarkMagenta },
            { ".7z", Color.DarkMagenta },
            { ".exe", Color.Red },
            { ".dll", Color.Gray },
            { "default", Color.Gray }
        };
    }

    private void InitContextMenu()
    {
        contextMenu = new ContextMenuStrip();
        contextMenu.Font = new Font("微软雅黑", 9);

        // 新增剪切、复制选项
        ToolStripMenuItem menuCut = new ToolStripMenuItem("剪切");
        menuCut.Click += (s, e) => CutItem();
        contextMenu.Items.Add(menuCut);

        ToolStripMenuItem menuCopy = new ToolStripMenuItem("复制");
        menuCopy.Click += (s, e) => CopyItem();
        contextMenu.Items.Add(menuCopy);

        ToolStripMenuItem menuPaste = new ToolStripMenuItem("粘贴");
        menuPaste.Click += (s, e) => PasteItem();
        contextMenu.Items.Add(menuPaste);

        contextMenu.Items.Add(new ToolStripSeparator());

        ToolStripMenuItem menuNewFolder = new ToolStripMenuItem("新建文件夹");
        menuNewFolder.Click += MenuNewFolder_Click;
        contextMenu.Items.Add(menuNewFolder);

        ToolStripMenuItem menuNewFile = new ToolStripMenuItem("新建文件");
        menuNewFile.Click += MenuNewFile_Click;
        contextMenu.Items.Add(menuNewFile);

        contextMenu.Items.Add(new ToolStripSeparator());

        menuOpenFolder = new ToolStripMenuItem("打开文件夹");
        menuOpenFolder.Click += MenuOpenFolder_Click;
        contextMenu.Items.Add(menuOpenFolder);

        contextMenu.Items.Add(new ToolStripSeparator());

        ToolStripMenuItem menuRename = new ToolStripMenuItem("重命名");
        menuRename.Click += MenuRename_Click;
        contextMenu.Items.Add(menuRename);

        contextMenu.Items.Add(new ToolStripSeparator());

        ToolStripMenuItem menuDelete = new ToolStripMenuItem("删除");
        menuDelete.Click += MenuDelete_Click;
        contextMenu.Items.Add(menuDelete);
    }

    private void TreeViewDirectory_DrawNode(object sender, DrawTreeNodeEventArgs e)
    {
        Font nodeFont = treeViewDirectory.Font;
        Color textColor = e.Node.ForeColor;
        Color backgroundColor = SystemColors.Window;

        if (highlightedNodes.Contains(e.Node))
        {
            backgroundColor = Color.Yellow;
        }
        else if (contentHighlightedNodes.Contains(e.Node))
        {
            backgroundColor = Color.Orange;
        }

        if (e.Node.Tag != null)
        {
            string path = e.Node.Tag.ToString();
            if (File.Exists(path))
            {
                string ext = Path.GetExtension(path);
                if (fileTypeColorMap.ContainsKey(ext))
                {
                    textColor = fileTypeColorMap[ext];
                }
                else
                {
                    textColor = fileTypeColorMap["default"];
                }
            }
            else if (Directory.Exists(path))
            {
                textColor = Color.FromArgb(52, 77, 221);
            }
            else if (e.Node.Text == "【权限不足，无法访问】")
            {
                textColor = Color.Gray;
            }
        }

        if ((e.State & TreeNodeStates.Selected) != 0)
        {
            e.Graphics.FillRectangle(SystemBrushes.Highlight, e.Bounds);
            TextRenderer.DrawText(
                e.Graphics,
                e.Node.Text,
                nodeFont,
                e.Bounds,
                SystemColors.HighlightText,
                TextFormatFlags.GlyphOverhangPadding
            );
        }
        else
        {
            e.Graphics.FillRectangle(new SolidBrush(backgroundColor), e.Bounds);
            TextRenderer.DrawText(
                e.Graphics,
                e.Node.Text,
                nodeFont,
                e.Bounds,
                textColor,
                TextFormatFlags.GlyphOverhangPadding
            );
        }

        e.DrawDefault = false;
    }

    private void TreeViewDirectory_NodeMouseDoubleClick(object sender, TreeNodeMouseClickEventArgs e)
    {
        if (e.Node.Tag == null) return;
        string path = e.Node.Tag.ToString();

        if (File.Exists(path))
        {
            try
            {
                string ext = Path.GetExtension(path).ToLower();
                // 统一封装路径，添加双引号处理特殊字符
                string quotedPath = string.Format("\"{0}\"", path);

                switch (ext)
                {
                    case ".txt":
                        System.Diagnostics.Process.Start("notepad.exe", quotedPath);
                        break;
                    case ".md":
                        System.Diagnostics.Process.Start("typora.exe", quotedPath);
                        break;
                    case ".xls":
                    case ".xlsx":
                        System.Diagnostics.Process.Start("wps.exe", quotedPath);
                        break;
                    default:
                        // 对于未知类型，使用系统默认程序打开（推荐用ProcessStartInfo方式更稳定）
                        ProcessStartInfo psi = new ProcessStartInfo
                        {
                            FileName = path,
                            UseShellExecute = true
                        };
                        Process.Start(psi);
                        break;
                }
            }
            catch (Exception ex)
            {
                MessageBox.Show(string.Format("打开文件失败：{0}", ex.Message), "错误", MessageBoxButtons.OK, MessageBoxIcon.Error);
            }
        }
    }

    private void TreeViewDirectory_MouseClick(object sender, MouseEventArgs e)
    {
        if (e.Button == MouseButtons.Right)
        {
            TreeNode hitNode = treeViewDirectory.GetNodeAt(e.X, e.Y);
            if (hitNode != null && hitNode.Tag != null)
            {
                treeViewDirectory.SelectedNode = hitNode;
                string path = hitNode.Tag.ToString();

                if (Directory.Exists(path))
                {
                    menuOpenFolder.Visible = true;
                }
                else if (File.Exists(path))
                {
                    menuOpenFolder.Visible = false;
                }
                else
                {
                    menuOpenFolder.Visible = false;
                }

                contextMenu.Show(treeViewDirectory, e.Location);
            }
        }
    }

    private void TreeViewDirectory_NodeMouseClick(object sender, TreeNodeMouseClickEventArgs e)
    {
        if (e.Node.Tag == null) return;
        if (e.Button == MouseButtons.Left)
        {
            string path = e.Node.Tag.ToString();
            if (Directory.Exists(path))
            {
                e.Node.Toggle();
            }
        }
    }

    private void MenuOpenFolder_Click(object sender, EventArgs e)
    {
        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null || selectedNode.Tag == null) return;

        string path = selectedNode.Tag.ToString();
        if (Directory.Exists(path))
        {
            try
            {
                // 路径包裹双引号，处理特殊字符
                System.Diagnostics.Process.Start("explorer.exe", string.Format("\"{0}\"", path));
            }
            catch (Exception ex)
            {
                MessageBox.Show(string.Format("打开文件夹失败：{0}", ex.Message), "错误", MessageBoxButtons.OK, MessageBoxIcon.Error);
            }
        }
    }

    private void MenuNewFolder_Click(object sender, EventArgs e)
    {
        if (string.IsNullOrEmpty(rootFolderPath))
        {
            ShowNotification("请先选择根目录！", Color.Orange);
            return;
        }

        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null) return;

        string parentPath = selectedNode.Tag.ToString();
        if (File.Exists(parentPath))
        {
            parentPath = Path.GetDirectoryName(parentPath);
        }

        string folderName = PromptForFolderName();
        if (string.IsNullOrEmpty(folderName)) return;

        string newFolderPath = Path.Combine(parentPath, folderName);
        int index = 1;
        while (Directory.Exists(newFolderPath))
        {
            newFolderPath = Path.Combine(parentPath, string.Format("{0}{1}", folderName, index));
            index++;
        }

        try
        {
            Directory.CreateDirectory(newFolderPath);
            // 刷新后定位到父级
            RefreshTreeView(parentPath);
            SelectFileInTreeView(newFolderPath);
            ShowNotification("文件夹创建成功！");
        }
        catch (Exception ex)
        {
            ShowNotification(string.Format("创建文件夹失败：{0}", ex.Message), Color.Red);
        }
    }

    private string PromptForFolderName()
    {
        using (Form prompt = new Form())
        {
            prompt.Width = 300;
            prompt.Height = 150;
            prompt.Text = "新建文件夹";
            prompt.StartPosition = FormStartPosition.CenterParent;

            Label textLabel = new Label() { Left = 10, Top = 20, Text = "请输入文件夹名称：" };
            TextBox textBox = new TextBox() { Left = 10, Top = 50, Width = 260 };

            Button confirmation = new Button() { Text = "确定", Left = 200, Width = 70, Top = 80, DialogResult = DialogResult.OK };
            confirmation.Click += (sender, e) => { prompt.Close(); };

            prompt.Controls.Add(textLabel);
            prompt.Controls.Add(textBox);
            prompt.Controls.Add(confirmation);
            prompt.AcceptButton = confirmation;

            return prompt.ShowDialog(this) == DialogResult.OK ? textBox.Text : null;
        }
    }

    private void MenuNewFile_Click(object sender, EventArgs e)
    {
        if (string.IsNullOrEmpty(rootFolderPath))
        {
            ShowNotification("请先选择根目录！", Color.Orange);
            return;
        }

        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null) return;

        string parentPath = selectedNode.Tag.ToString();
        if (File.Exists(parentPath))
        {
            parentPath = Path.GetDirectoryName(parentPath);
        }

        string fileName;
        string extension;
        if (!PromptForFileNameAndType(out fileName, out extension)) return;

        string newFilePath = Path.Combine(parentPath, fileName + extension);
        int index = 1;
        while (File.Exists(newFilePath))
        {
            newFilePath = Path.Combine(parentPath, string.Format("{0}{1}{2}", fileName, index, extension));
            index++;
        }

        try
        {
            File.Create(newFilePath).Close();
            // 刷新后定位到父级
            RefreshTreeView(parentPath);
            SelectFileInTreeView(newFilePath);
            ShowNotification("文件创建成功！");
        }
        catch (Exception ex)
        {
            ShowNotification(string.Format("创建文件失败：{0}", ex.Message), Color.Red);
        }
    }

    private void SelectFileInTreeView(string filePath)
    {
        TreeNode node = FindNodeByPath(treeViewDirectory.Nodes, filePath);
        if (node != null)
        {
            treeViewDirectory.SelectedNode = node;
            treeViewDirectory.SelectedNode.EnsureVisible();
        }
    }

    private TreeNode FindNodeByPath(TreeNodeCollection nodes, string path)
    {
        foreach (TreeNode node in nodes)
        {
            if (node.Tag != null && node.Tag.ToString() == path)
            {
                return node;
            }

            TreeNode foundNode = FindNodeByPath(node.Nodes, path);
            if (foundNode != null)
            {
                node.Expand();
                return foundNode;
            }
        }
        return null;
    }

    private bool PromptForFileNameAndType(out string fileName, out string extension)
    {
        using (Form prompt = new Form())
        {
            prompt.Width = 300;
            prompt.Height = 250;
            prompt.Text = "新建文件";
            prompt.StartPosition = FormStartPosition.CenterParent;

            Label nameLabel = new Label() { Left = 10, Top = 20, Text = "请输入文件名：" };
            TextBox nameBox = new TextBox() { Left = 10, Top = 50, Width = 260 };

            Label typeLabel = new Label() { Left = 10, Top = 90, Text = "请选择文件类型：" };
            ComboBox comboBox = new ComboBox() { Left = 10, Top = 120, Width = 260 };
            comboBox.Items.AddRange(new string[] { "Markdown 文件 (*.md)", "文本文件 (*.txt)", "表格文件 (*.xls)" });
            comboBox.SelectedIndex = 0;

            Button confirmation = new Button() { Text = "确定", Left = 200, Width = 70, Top = 170, DialogResult = DialogResult.OK };
            confirmation.Click += (sender, e) => { prompt.Close(); };

            prompt.Controls.Add(nameLabel);
            prompt.Controls.Add(nameBox);
            prompt.Controls.Add(typeLabel);
            prompt.Controls.Add(comboBox);
            prompt.Controls.Add(confirmation);
            prompt.AcceptButton = confirmation;

            if (prompt.ShowDialog(this) == DialogResult.OK)
            {
                fileName = nameBox.Text;
                extension = comboBox.SelectedItem.ToString().Contains("*.txt") ? ".txt" :
                            comboBox.SelectedItem.ToString().Contains("*.md") ? ".md" : ".xls";
                return true;
            }
            else
            {
                fileName = null;
                extension = null;
                return false;
            }
        }
    }

    private string PromptForNewName(string currentName)
    {
        using (Form prompt = new Form())
        {
            prompt.Width = 300;
            prompt.Height = 150;
            prompt.Text = "重命名";
            prompt.StartPosition = FormStartPosition.CenterParent;

            Label textLabel = new Label() { Left = 10, Top = 20, Text = "请输入新的名称：" };
            TextBox textBox = new TextBox() { Left = 10, Top = 50, Width = 260, Text = currentName };

            Button confirmation = new Button() { Text = "确定", Left = 200, Width = 70, Top = 80, DialogResult = DialogResult.OK };
            confirmation.Click += (sender, e) => { prompt.Close(); };

            prompt.Controls.Add(textLabel);
            prompt.Controls.Add(textBox);
            prompt.Controls.Add(confirmation);
            prompt.AcceptButton = confirmation;

            return prompt.ShowDialog(this) == DialogResult.OK ? textBox.Text : currentName;
        }
    }

    private void MenuRename_Click(object sender, EventArgs e)
    {
        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null || selectedNode.Tag == null) return;

        string path = selectedNode.Tag.ToString();
        string newName = PromptForNewName(selectedNode.Text);

        if (string.IsNullOrEmpty(newName) || newName == selectedNode.Text)
        {
            return;
        }

        string newPath = Path.Combine(Path.GetDirectoryName(path), newName);
        string parentPath = Path.GetDirectoryName(path);

        try
        {
            if (Directory.Exists(path))
            {
                Directory.Move(path, newPath);
            }
            else if (File.Exists(path))
            {
                File.Move(path, newPath);
            }
            // 刷新后定位到父级
            RefreshTreeView(parentPath);
            SelectFileInTreeView(newPath);
            ShowNotification("重命名成功！");
        }
        catch (Exception ex)
        {
            ShowNotification(string.Format("重命名失败：{0}", ex.Message), Color.Red);
        }
    }

    private void MenuDelete_Click(object sender, EventArgs e)
    {
        TreeNode selectedNode = treeViewDirectory.SelectedNode;
        if (selectedNode == null || selectedNode.Tag == null) return;

        string path = selectedNode.Tag.ToString();
        // 记录父级路径（如果是根目录，父级就是自身）
        string parentPath = Path.GetDirectoryName(path);
        // 处理根目录的情况：如果删除的是根目录下的直接子项，父级是根目录
        if (string.IsNullOrEmpty(parentPath) || parentPath == rootFolderPath)
        {
            parentPath = rootFolderPath;
        }

        DialogResult result = MessageBox.Show(string.Format("确定要删除：{0}？", path), "确认删除", MessageBoxButtons.YesNo, MessageBoxIcon.Warning);
        if (result != DialogResult.Yes) return;

        try
        {
            if (Directory.Exists(path))
            {
                Directory.Delete(path, true);
            }
            else if (File.Exists(path))
            {
                File.Delete(path);
            }
            // 刷新后定位到父级目录
            RefreshTreeView(parentPath);
            SelectFileInTreeView(parentPath);
            ShowNotification("删除成功！");
        }
        catch (Exception ex)
        {
            ShowNotification(string.Format("删除失败：{0}", ex.Message), Color.Red);
        }
    }

    private void LoadConfigAndInitDirectory()
    {
        if (File.Exists(configFilePath))
        {
            string savedPath = File.ReadAllText(configFilePath, Encoding.UTF8).Trim();
            if (Directory.Exists(savedPath))
            {
                rootFolderPath = savedPath;
                treeViewDirectory.Nodes.Clear();
                LoadDirectoryToTreeView(rootFolderPath, treeViewDirectory.Nodes);
                treeViewDirectory.Nodes[0].Expand();
                this.Text = string.Format("RootTrees - root：{0}", rootFolderPath);
            }
        }
    }

    private void SaveConfig(string folderPath)
    {
        File.WriteAllText(configFilePath, folderPath, Encoding.UTF8);
    }

    private void BtnSelectFolder_Click(object sender, EventArgs e)
    {
        using (FolderBrowserDialog folderDialog = new FolderBrowserDialog())
        {
            folderDialog.Description = "请选择要管理的根目录";
            if (!string.IsNullOrEmpty(rootFolderPath) && Directory.Exists(rootFolderPath))
            {
                folderDialog.SelectedPath = rootFolderPath;
            }
            if (folderDialog.ShowDialog() == DialogResult.OK)
            {
                rootFolderPath = folderDialog.SelectedPath;
                treeViewDirectory.Nodes.Clear();
                LoadDirectoryToTreeView(rootFolderPath, treeViewDirectory.Nodes);
                treeViewDirectory.Nodes[0].Expand();
                this.Text = string.Format("RootTrees - root：{0}", rootFolderPath);
                SaveConfig(rootFolderPath);
            }
        }
    }

    private void LoadDirectoryToTreeView(string path, TreeNodeCollection parentNodes)
    {
        try
        {
            TreeNode folderNode = new TreeNode(Path.GetFileName(path));
            folderNode.Tag = path;
            folderNode.ImageIndex = 0;
            parentNodes.Add(folderNode);

            string[] subFolders = Directory.GetDirectories(path);
            foreach (string subFolder in subFolders)
            {
                LoadDirectoryToTreeView(subFolder, folderNode.Nodes);
            }

            string[] files = Directory.GetFiles(path);
            foreach (string file in files)
            {
                TreeNode fileNode = new TreeNode(Path.GetFileName(file));
                fileNode.Tag = file;
                fileNode.ImageIndex = 1;
                folderNode.Nodes.Add(fileNode);
            }
        }
        catch (UnauthorizedAccessException)
        {
            TreeNode errorNode = new TreeNode("【权限不足，无法访问】");
            parentNodes.Add(errorNode);
        }
        catch (Exception ex)
        {
            MessageBox.Show(string.Format("加载目录失败：{0}", ex.Message), "错误", MessageBoxButtons.OK, MessageBoxIcon.Error);
        }
    }

    // 重载1：默认刷新（定位到根目录）
    private void RefreshTreeView()
    {
        RefreshTreeView(rootFolderPath);
    }

    // 重载2：刷新后定位到指定路径
    private void RefreshTreeView(string targetPath)
    {
        if (!string.IsNullOrEmpty(rootFolderPath))
        {
            treeViewDirectory.Nodes.Clear();
            LoadDirectoryToTreeView(rootFolderPath, treeViewDirectory.Nodes);
            // 找到并展开目标路径的节点
            TreeNode targetNode = FindNodeByPath(treeViewDirectory.Nodes, targetPath);
            if (targetNode != null)
            {
                targetNode.Expand();
                treeViewDirectory.SelectedNode = targetNode;
                targetNode.EnsureVisible();
            }
            else
            {
                // 如果目标路径不存在，展开根节点
                treeViewDirectory.Nodes[0].Expand();
            }
        }
    }

    // 搜索按钮点击事件
    private void BtnSearch_Click(object sender, EventArgs e)
    {
        if (btnSearch.Text == "搜索")
        {
            string searchTerm = PromptForSearchTerm();
            if (!string.IsNullOrEmpty(searchTerm))
            {
                SearchAndHighlightNodes(treeViewDirectory.Nodes, searchTerm);
                btnSearch.Text = "取消";
            }
        }
        else
        {
            ClearHighlight();
            btnSearch.Text = "搜索";
        }
    }

    // 执行搜索
    private void SearchAndHighlightNodes(TreeNodeCollection nodes, string searchTerm)
    {
        foreach (TreeNode node in nodes)
        {
            bool isTitleMatch = false;
            bool isContentMatch = false;

            // 检查节点文本是否包含搜索关键字
            if (node.Text.IndexOf(searchTerm, StringComparison.OrdinalIgnoreCase) >= 0)
            {
                isTitleMatch = true;
                highlightedNodes.Add(node);
            }
            else if (node.Tag != null && File.Exists(node.Tag.ToString()))
            {
                // 如果是文件，读取文件内容并检查是否包含搜索关键字
                string filePath = node.Tag.ToString();
                string ext = Path.GetExtension(filePath).ToLower();

                // 仅对文本文件进行内容搜索
                if (ext == ".txt" || ext == ".md" || ext == ".cs" || ext == ".java" || ext == ".py" || ext == ".html" || ext == ".js" || ext == ".css")
                {
                    try
                    {
                        string content = File.ReadAllText(filePath);
                        if (content.IndexOf(searchTerm, StringComparison.OrdinalIgnoreCase) >= 0)
                        {
                            isContentMatch = true;
                            contentHighlightedNodes.Add(node);
                        }
                    }
                    catch (Exception ex)
                    {
                        MessageBox.Show(string.Format("读取文件失败：{0}", ex.Message), "错误", MessageBoxButtons.OK, MessageBoxIcon.Error);
                    }
                }
            }

            if (isTitleMatch || isContentMatch)
            {
                node.EnsureVisible();
            }

            // 递归搜索子节点
            SearchAndHighlightNodes(node.Nodes, searchTerm);
        }

        treeViewDirectory.Invalidate();
    }

    // 弹出对话框输入搜索关键字
    private string PromptForSearchTerm()
    {
        using (Form prompt = new Form())
        {
            prompt.Width = 300;
            prompt.Height = 150;
            prompt.Text = "搜索";
            prompt.StartPosition = FormStartPosition.CenterParent;

            Label textLabel = new Label() { Left = 10, Top = 20, Text = "请输入搜索关键字：" };
            TextBox textBox = new TextBox() { Left = 10, Top = 50, Width = 260 };

            Button confirmation = new Button() { Text = "确定", Left = 200, Width = 70, Top = 80, DialogResult = DialogResult.OK };
            confirmation.Click += (sender, e) => { prompt.Close(); };

            prompt.Controls.Add(textLabel);
            prompt.Controls.Add(textBox);
            prompt.Controls.Add(confirmation);
            prompt.AcceptButton = confirmation;

            return prompt.ShowDialog(this) == DialogResult.OK ? textBox.Text : null;
        }
    }

    // 清除高亮
    private void ClearHighlight()
    {
        highlightedNodes.Clear();
        contentHighlightedNodes.Clear();
        treeViewDirectory.Invalidate();
    }

    [STAThread]
    static void Main()
    {
        Application.EnableVisualStyles();
        Application.SetCompatibleTextRenderingDefault(false);
        Application.Run(new FileManagerForm());
    }
}

// 新增：时间戳转换窗体
public class TimestampConverterForm : Form
{
    private TextBox txtCurrentTs;
    private Label lblUnit;
    private Button btnSwitchUnit;
    private Button btnCopy;
    private TextBox txtTsInput;
    private ComboBox cboTsUnit;
    private Button btnTsToDate;
    private TextBox txtTsResult;
    private TextBox txtDateInput;
    private Button btnDateToTs;
    private TextBox txtDateResult;
    private ComboBox cboDateUnit;
    private Label lblStatus;
    private Timer timerStatus;
    private string currentUnit = "ms"; // 默认毫秒单位

    public TimestampConverterForm()
    {
        // 窗体设置
        this.Text = "时间戳转换工具";
        this.Size = new Size(800, 500);
        this.StartPosition = FormStartPosition.CenterParent;
        this.Font = new Font("微软雅黑", 9);
        this.MaximizeBox = false;
        this.MinimizeBox = false;

        // 初始化控件
        InitControls();
        // 初始化当前时间戳
        UpdateCurrentTs();
        // 每秒更新当前时间戳
        Timer timer = new Timer { Interval = 1000 };
        timer.Tick += (s, e) => UpdateCurrentTs();
        timer.Start();
        // 初始化日期输入框
        InitDateInput();
    }

    private void InitControls()
    {
        // 布局面板
        TableLayoutPanel mainPanel = new TableLayoutPanel
        {
            Dock = DockStyle.Fill,
            Padding = new Padding(20),
            ColumnCount = 3,
            RowCount = 10
        };
        mainPanel.ColumnStyles.Add(new ColumnStyle(SizeType.Percent, 70F));
        mainPanel.ColumnStyles.Add(new ColumnStyle(SizeType.Percent, 15F));
        mainPanel.ColumnStyles.Add(new ColumnStyle(SizeType.Percent, 15F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 30F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 40F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 30F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 10F)); // 间隔
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 30F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 40F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 10F)); // 间隔
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 30F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Absolute, 40F));
        mainPanel.RowStyles.Add(new RowStyle(SizeType.Percent, 100F));

        // 1. 当前时间戳区域
        Label lblCurrentTs = new Label { Text = "当前时间戳", Font = new Font("微软雅黑", 10, FontStyle.Bold) };
        mainPanel.Controls.Add(lblCurrentTs, 0, 0);
        mainPanel.SetColumnSpan(lblCurrentTs, 3);

        txtCurrentTs = new TextBox { ReadOnly = true };
        mainPanel.Controls.Add(txtCurrentTs, 0, 1);

        lblUnit = new Label { Text = "毫秒", TextAlign = ContentAlignment.MiddleCenter };
        mainPanel.Controls.Add(lblUnit, 1, 1);

        btnSwitchUnit = new Button { Text = "切换单位" };
        btnSwitchUnit.Click += BtnSwitchUnit_Click;
        mainPanel.Controls.Add(btnSwitchUnit, 0, 2);

        btnCopy = new Button { Text = "复制" };
        btnCopy.Click += BtnCopy_Click;
        mainPanel.Controls.Add(btnCopy, 1, 2);

        // 状态提示标签
        lblStatus = new Label
        {
            Text = "",
            ForeColor = Color.White,
            BackColor = Color.Green,
            TextAlign = ContentAlignment.MiddleCenter,
            Visible = false,
            BorderStyle = BorderStyle.FixedSingle
        };
        mainPanel.Controls.Add(lblStatus, 2, 1);
        mainPanel.SetRowSpan(lblStatus, 2);

        // 2. 时间戳转日期区域
        Label lblTsToDate = new Label { Text = "时间戳转日期", Font = new Font("微软雅黑", 10, FontStyle.Bold) };
        mainPanel.Controls.Add(lblTsToDate, 0, 4);
        mainPanel.SetColumnSpan(lblTsToDate, 3);

        txtTsInput = new TextBox { PlaceholderText = "输入时间戳" };
        mainPanel.Controls.Add(txtTsInput, 0, 5);

        cboTsUnit = new ComboBox();
        cboTsUnit.Items.AddRange(new string[] { "毫秒", "秒" });
        cboTsUnit.SelectedIndex = 0;
        mainPanel.Controls.Add(cboTsUnit, 1, 5);

        btnTsToDate = new Button { Text = "转换" };
        btnTsToDate.Click += BtnTsToDate_Click;
        mainPanel.Controls.Add(btnTsToDate, 2, 5);

        txtTsResult = new TextBox { ReadOnly = true };
        mainPanel.Controls.Add(txtTsResult, 0, 6);
        mainPanel.SetColumnSpan(txtTsResult, 3);

        // 3. 日期转时间戳区域
        Label lblDateToTs = new Label { Text = "日期转时间戳", Font = new Font("微软雅黑", 10, FontStyle.Bold) };
        mainPanel.Controls.Add(lblDateToTs, 0, 7);
        mainPanel.SetColumnSpan(lblDateToTs, 3);

        txtDateInput = new TextBox { PlaceholderText = "例如:2023-10-01 12:30:45" };
        mainPanel.Controls.Add(txtDateInput, 0, 8);

        btnDateToTs = new Button { Text = "转换" };
        btnDateToTs.Click += BtnDateToTs_Click;
        mainPanel.Controls.Add(btnDateToTs, 1, 8);

        txtDateResult = new TextBox { ReadOnly = true };
        mainPanel.Controls.Add(txtDateResult, 0, 9);

        cboDateUnit = new ComboBox();
        cboDateUnit.Items.AddRange(new string[] { "毫秒", "秒" });
        cboDateUnit.SelectedIndex = 0;
        mainPanel.Controls.Add(cboDateUnit, 1, 9);

        // 提示标签
        Label lblTip = new Label
        {
            Text = "支持格式:YYYY-MM-DD HH:MM:SS、YYYY/MM/DD HH:MM:SS",
            Font = new Font("微软雅黑", 8),
            ForeColor = Color.Gray
        };
        mainPanel.Controls.Add(lblTip, 0, 10);
        mainPanel.SetColumnSpan(lblTip, 3);

        this.Controls.Add(mainPanel);

        // 初始化状态定时器
        timerStatus = new Timer { Interval = 2000 };
        timerStatus.Tick += (s, e) =>
        {
            lblStatus.Visible = false;
            timerStatus.Stop();
        };
    }

    // 初始化日期输入框
    private void InitDateInput()
    {
        DateTime now = DateTime.Now;
        txtDateInput.Text = now.ToString("yyyy-MM-dd HH:mm:ss");
    }

    // 更新当前时间戳
    private void UpdateCurrentTs()
    {
        long ts = currentUnit == "ms" ? DateTime.Now.Ticks / 10000 : DateTime.Now.Ticks / 10000000;
        txtCurrentTs.Text = ts.ToString();
    }

    // 显示状态提示
    private void ShowStatus(string message, Color backColor = default(Color))
    {
        lblStatus.Text = message;
        lblStatus.BackColor = backColor == default(Color) ? Color.Green : backColor;
        lblStatus.Visible = true;
        timerStatus.Stop();
        timerStatus.Start();
    }

    // 切换单位按钮事件
    private void BtnSwitchUnit_Click(object sender, EventArgs e)
    {
        currentUnit = currentUnit == "ms" ? "s" : "ms";
        lblUnit.Text = currentUnit == "ms" ? "毫秒" : "秒";
        UpdateCurrentTs();
        ShowStatus($"已切换为{lblUnit.Text}");
    }

    // 复制按钮事件
    private void BtnCopy_Click(object sender, EventArgs e)
    {
        Clipboard.SetText(txtCurrentTs.Text);
        ShowStatus("已复制到剪贴板");
    }

    // 时间戳转日期按钮事件
    private void BtnTsToDate_Click(object sender, EventArgs e)
    {
        if (string.IsNullOrEmpty(txtTsInput.Text) || !long.TryParse(txtTsInput.Text, out long ts))
        {
            ShowStatus("请输入有效的时间戳", Color.Orange);
            return;
        }

        string unit = cboTsUnit.SelectedItem.ToString();
        long timestamp = unit == "毫秒" ? ts : ts * 1000;

        try
        {
            DateTime date = new DateTime(1970, 1, 1, 0, 0, 0, DateTimeKind.Utc).AddMilliseconds(timestamp).ToLocalTime();
            txtTsResult.Text = date.ToString("yyyy-MM-dd HH:mm:ss");
        }
        catch
        {
            ShowStatus("时间戳超出范围", Color.Red);
        }
    }

    // 日期转时间戳按钮事件
    private void BtnDateToTs_Click(object sender, EventArgs e)
    {
        if (string.IsNullOrEmpty(txtDateInput.Text))
        {
            ShowStatus("请输入日期", Color.Orange);
            return;
        }

        DateTime date;
        // 尝试多种格式解析
        string[] formats = { "yyyy-MM-dd HH:mm:ss", "yyyy/MM/dd HH:mm:ss", "yyyy-MM-dd", "yyyy/MM/dd" };
        if (!DateTime.TryParseExact(txtDateInput.Text, formats, CultureInfo.InvariantCulture, DateTimeStyles.None, out date))
        {
            ShowStatus("日期格式不正确", Color.Red);
            return;
        }

        // 转换为时间戳（1970-01-01 00:00:00 UTC）
        DateTime epoch = new DateTime(1970, 1, 1, 0, 0, 0, DateTimeKind.Utc);
        long ts = (long)(date.ToUniversalTime() - epoch).TotalMilliseconds;

        string unit = cboDateUnit.SelectedItem.ToString();
        long result = unit == "毫秒" ? ts : ts / 1000;
        txtDateResult.Text = result.ToString();
    }
}